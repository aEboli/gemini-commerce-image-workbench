# 进展记录

## 2026-03-14

## 阶段：OpenSpec 与 Context7 安装前摸底

- 阶段目标：确认 `OpenSpec` 与 `Context7` 的官方安装方式、本机可用运行环境，以及当前 Codex 配置中需要兼容或修改的部分，为后续安装和配置做准备。
- 已完成事项：
  - 查阅 `https://github.com/Fission-AI/OpenSpec` 与 `https://github.com/upstash/context7` 的官方 README，确认 `OpenSpec` 官方推荐通过全局安装 `@fission-ai/openspec` 后在目标仓库执行 `openspec init`，`Context7` 官方推荐通过 `npx ctx7 setup` 或手动 MCP 配置接入客户端。
  - 确认当前机器具备可用的 Node.js 与 Git 环境：`node -v` 返回 `v24.13.1`，`git --version` 返回 `2.53.0.windows.1`，并确认可通过 `C:\Program Files\nodejs\npm.cmd` 调用 npm。
  - 检查当前 `C:\Users\AEboli\.codex\config.toml`，确认已存在多个 MCP 服务配置，可在此基础上追加 `Context7` 的 Codex 级配置。
  - 检查当前仓库根目录 `AGENTS.md`，确认仓库已存在自定义协作规则，后续接入 `OpenSpec` 时需要避免粗暴覆盖并处理兼容方案。
- 涉及文件：
  - `doc/进展记录.md`
  - `AGENTS.md`
  - `C:\Users\AEboli\.codex\config.toml`
- 待办事项或风险：
  - `OpenSpec` 初始化大概率会写入或更新根目录 `AGENTS.md`，需要先确认生成内容，再与现有协作规则做兼容合并。
  - `Context7` 如走远程 MCP 最佳实践，可能需要用户提供 API Key 或在安装过程中完成 OAuth 授权。
  - 由于当前终端 PowerShell 执行策略限制，后续 npm/npx 命令需要优先通过 `npm.cmd` / `npx.cmd` 调用。

## 阶段：OpenSpec 接入 Codex 工作区

- 阶段目标：把 `OpenSpec` 正式接入当前仓库，并为 Codex 补齐可直接调用的 OpenSpec 技能与 `/opsx-*` 提示命令。
- 已完成事项：
  - 通过 `npm install -g @fission-ai/openspec@1.2.0` 完成 `OpenSpec CLI` 全局安装。
  - 先在 `tmp/openspec-init-preview/` 做初始化预演，确认 `Codex` 模式下不会覆盖根目录 `AGENTS.md`，而是主要创建仓库内的 `openspec/` 与 `.codex/skills/openspec-*`。
  - 在仓库根目录执行 `openspec init --tools codex .`，成功生成 `openspec/changes`、`openspec/specs` 目录，以及 `openspec-propose`、`openspec-explore`、`openspec-apply-change`、`openspec-archive-change` 四个本地技能。
  - 确认全局 `C:\Users\AEboli\.codex\prompts\` 下已生成 `opsx-propose.md`、`opsx-explore.md`、`opsx-apply.md`、`opsx-archive.md`，说明 Codex 侧的 `/opsx-*` 提示文件已经补齐。
- 涉及文件：
  - `doc/进展记录.md`
  - `.codex/skills/openspec-propose/SKILL.md`
  - `.codex/skills/openspec-explore/SKILL.md`
  - `.codex/skills/openspec-apply-change/SKILL.md`
  - `.codex/skills/openspec-archive-change/SKILL.md`
  - `openspec/changes/`
  - `openspec/specs/`
  - `C:\Users\AEboli\.codex\prompts\opsx-propose.md`
  - `C:\Users\AEboli\.codex\prompts\opsx-explore.md`
  - `C:\Users\AEboli\.codex\prompts\opsx-apply.md`
  - `C:\Users\AEboli\.codex\prompts\opsx-archive.md`
- 待办事项或风险：
  - `OpenSpec` 官方提示需要重启 IDE 后，`/opsx-*` slash commands 才会稳定生效，后续完成全部配置后需要提醒用户重启 Codex。
  - 本次初始化在非交互模式下跳过了额外配置项，当前以默认 workflow 为主；如后续需要自定义 schema 或 profile，可再单独补配。

## 阶段：Context7 MCP 接入与 OpenSpec Windows 兼容处理

- 阶段目标：把 `Context7` 以 Codex 可识别的 MCP 方式接入全局配置，同时修正 `OpenSpec` 在当前 Windows PowerShell 环境中的命令调用兼容问题。
- 已完成事项：
  - 通过 `npx @upstash/context7-mcp@2.1.4 --help` 确认 `Context7 MCP` 的本地启动参数，随后将其以 `npx.cmd` 形式写入 `C:\Users\AEboli\.codex\config.toml` 的 `mcp_servers.context7` 配置段。
  - 确认更新后的 `config.toml` 已包含 `Context7` 服务定义：`command = "C:\\Program Files\\nodejs\\npx.cmd"`，`args = ["-y", "@upstash/context7-mcp@2.1.4"]`。
  - 额外执行了 `Context7` 的启动可用性检查，确认服务进程能够以 `stdio` 模式启动并输出运行 banner；更深层的 MCP 握手测试在当前终端自动化环境下未拿到响应，因此保留为重启 Codex 后的实际接入验证项。
  - 新增 `scripts/openspec.cmd` 包装器，统一通过 `%APPDATA%\\npm\\openspec.cmd` 调用 `OpenSpec CLI`，并默认设置 `OPENSPEC_TELEMETRY=0`，避免当前网络受限环境下的遥测报错噪音。
  - 调整 `.codex/skills/openspec-*` 四个本地技能说明，把 `openspec ...` 命令替换为 `.\scripts\openspec.cmd ...`，并将归档流程中的 Unix `mkdir` / `mv` 改为 PowerShell 版本，确保后续在此仓库中可直接执行。
- 涉及文件：
  - `doc/进展记录.md`
  - `scripts/openspec.cmd`
  - `.codex/skills/openspec-propose/SKILL.md`
  - `.codex/skills/openspec-explore/SKILL.md`
  - `.codex/skills/openspec-apply-change/SKILL.md`
  - `.codex/skills/openspec-archive-change/SKILL.md`
  - `C:\Users\AEboli\.codex\config.toml`
- 待办事项或风险：
  - `Context7` 的新 MCP 配置需要在重启 Codex 后才会被实际加载；当前会话内无法直接复用更新后的全局配置。
  - 当前以“无 API Key”模式接入 `Context7`；如后续需要更高额度、团队配额或私有认证方式，可再补配 `CONTEXT7_API_KEY`。
  - `Context7` 的自动化握手测试已经确认进程可启动，但未在当前脚本环境中拿到 `tools/list` 回包；如重启后 Codex 仍未识别该 MCP，再继续做定向排障。

## 阶段：OpenSpec 与 Context7 交付文档补充

- 阶段目标：为本次 `OpenSpec + Context7` 接入补齐面向后续协作者的使用说明与维护 PRD，降低后续接手成本。
- 已完成事项：
  - 新增 `Readme/使用说明-OpenSpec与Context7.md`，说明已安装组件、使用方式、重启要求、OpenSpec 命令入口、Context7 使用方式与可选 API Key 扩展。
  - 新增 `Readme/PRD-OpenSpec与Context7接入.md`，沉淀本次接入的背景、目标、非目标、功能需求、验收标准与风险假设。
  - 在本文档中记录新增交付文档文件名及用途，满足仓库协作规则要求。
- 涉及文件：
  - `doc/进展记录.md`
  - `Readme/使用说明-OpenSpec与Context7.md`
  - `Readme/PRD-OpenSpec与Context7接入.md`
- 待办事项或风险：
  - 当前文档基于“OpenSpec 已落地、Context7 待重启 Codex 完成最终加载验证”的状态编写；如果重启后行为与当前记录不一致，需要同步回写这些说明文档。

## 阶段：Context7 OAuth 模式切换与授权验证

- 阶段目标：将 `Context7` 从本地 `npx` 模式切换到官方远程 `OAuth` MCP 端点，并在当前机器上完成一次真实授权验证。
- 已完成事项：
  - 依据 Context7 官方 OAuth 文档，将 `C:\Users\AEboli\.codex\config.toml` 中的 `mcp_servers.context7` 从本地命令模式切换为远程端点 `https://mcp.context7.com/mcp/oauth`。
  - 通过 `codex mcp list` 与 `codex mcp get context7` 校验，确认 Codex CLI 已将 `context7` 识别为 `streamable_http` 远程 MCP，且初始状态为 `Not logged in`。
  - 执行 `codex mcp login context7` 启动 OAuth 流程，并成功完成授权。
  - 再次通过 `codex mcp list` 校验，确认当前机器上的 `context7` 认证状态已变为 `OAuth`。
  - 同步更新 `Readme/使用说明-OpenSpec与Context7.md` 与 `Readme/PRD-OpenSpec与Context7接入.md`，将 Context7 接入方式从“本地无 API Key”改为“远程 OAuth”。
- 涉及文件：
  - `doc/进展记录.md`
  - `Readme/使用说明-OpenSpec与Context7.md`
  - `Readme/PRD-OpenSpec与Context7接入.md`
  - `C:\Users\AEboli\.codex\config.toml`
- 待办事项或风险：
  - 当前机器已完成一次 OAuth 授权，但后续如果 Context7 令牌过期、被手动登出或客户端清空本地状态，仍需要重新执行一次 `codex mcp login context7`。
  - 虽然 Codex CLI 已确认 `context7` 为 `OAuth` 状态，但桌面端当前会话是否立即加载新配置，仍以重启后的实际表现为准。

## 阶段：Context7 端到端查询冒烟测试

- 阶段目标：确认新启动的 Codex 进程不只是“识别到 Context7 配置”，而是能够实际调用 Context7 完成一次真实文档查询。
- 已完成事项：
  - 使用 `codex exec` 拉起一个新的非交互 Codex 进程，强制其“只使用 Context7 MCP”查询 React 官方库信息。
  - 从执行结果确认新进程启动时已加载 `context7`，并成功调用 `context7.resolve-library-id`。
  - 查询结果返回 `CONTEXT7_STATUS=success`，并给出 `LIBRARY_ID=/facebook/react`，说明 OAuth 认证后的 Context7 已在新 Codex 进程中完成真实可用验证。
- 涉及文件：
  - `doc/进展记录.md`
  - `tmp/context7-smoketest.txt`
- 待办事项或风险：
  - 本次已通过新进程验证 `Context7` 可用，但当前桌面会话如果尚未重启，仍可能继续使用旧的 MCP 加载状态。

## 阶段：OpenSpec 端到端冒烟测试

- 阶段目标：确认 `OpenSpec` 不只是安装完成，而是同时具备“CLI 可用”和“Codex slash command 可用”两层能力。
- 已完成事项：
  - 通过 `.\scripts\openspec.cmd list` 验证本仓库的 OpenSpec Windows 包装器可正常工作。
  - 创建临时 change `smoke-test-openspec`，并通过 `.\scripts\openspec.cmd list` 与 `.\scripts\openspec.cmd status --change smoke-test-openspec --json` 确认 OpenSpec CLI 能正常创建、识别并返回 schema/artifact 状态。
  - 使用新的 `codex exec` 进程直接调用 `/opsx:explore`，得到返回 `OPENSPEC_SLASH=available`，说明 Codex 侧的 OpenSpec slash prompt 也已实际可用。
  - 为避免污染仓库，测试结束后已删除临时 change，当前 `.\scripts\openspec.cmd list` 再次返回 `No active changes found.`。
- 涉及文件：
  - `doc/进展记录.md`
  - `scripts/openspec.cmd`
  - `openspec/changes/`
  - `tmp/openspec-slash-smoketest.txt`
- 待办事项或风险：
  - 当前已确认 OpenSpec CLI 与 slash command 在新 Codex 进程中可用；如果桌面端当前会话还没有重启，可能仍暂时看不到最新加载状态。

## 阶段：当前工作区项目巡检与进度同步

- 阶段目标：快速确认当前文件夹内的主项目、配套文档、工作区未提交改动和 OpenSpec 落地状态，并把“文档记录的进度”与“当前工作区真实状态”对齐。
- 已完成事项：
  - 确认当前主项目为 `commerce-image-studio`（`v0.4.0`），技术栈为 `Next.js + React + TypeScript`，定位是“基于 Gemini 的电商生图工作台”，目录主干集中在 `app/`、`components/`、`lib/`、`data/`、`docs/`、`Readme/`。
  - 确认仓库内已补齐对外交付文档，`Readme/` 目录下已有电商 AI 出图工作台的使用说明、PRD、版本说明，以及 `PRD-Creater` 的使用说明与 PRD。
  - 对齐 `doc/进展记录.md` 与当前工作区改动，确认当前未提交的核心改动主要集中在以下方向：
    - 创建页数量输入改为允许临时删空，并把合法范围扩展到 `1-10`。
    - 服务端补充 `variantsPerType` 的整数范围校验。
    - 全站原生下拉补充深色主题样式。
    - 平台选项扩展到 `拼多多`、`Temu`、`SHEIN`。
    - 尺寸/重量信息补充双制归一化，并接入模板、翻译和提示词优化链路。
    - 创作页头部同步摘要进一步收口。
  - 确认上述改动与 `2026-03-13` 晚间已登记的几个阶段基本一致，说明这批能力已经完成实现和验证记录，但当前 Git 工作区里仍保留未提交状态。
  - 核对 `openspec/` 目录，当前已完成 OpenSpec 初始化框架接入，但 `openspec/changes/` 与 `openspec/specs/` 下暂未看到正在推进中的具体变更文件，说明目前更多是工具能力已就位，尚未在仓库内形成活跃 spec/change 条目。
  - 核对 Git 状态，当前存在 7 个已修改未提交文件：`app/api/generate/route.ts`、`app/globals.css`、`app/ui-ux-pro-max.css`、`components/create-job-form.tsx`、`lib/constants.ts`、`lib/gemini.ts`、`lib/templates.ts`；另有 `Readme/PRD-PRD-Creater.md`、`Readme/使用说明-PRD-Creater.md` 与 `doc/` 目录处于未跟踪状态。
- 涉及文件：
  - `package.json`
  - `README.md`
  - `doc/进展记录.md`
  - `app/api/generate/route.ts`
  - `app/globals.css`
  - `app/ui-ux-pro-max.css`
  - `components/create-job-form.tsx`
  - `lib/constants.ts`
  - `lib/gemini.ts`
  - `lib/templates.ts`
- 待办事项或风险：
  - 当前这批与创建页数量、平台扩展、双制尺寸和翻译补救相关的改动虽然已有阶段记录，但仍未形成一次干净提交；如果准备继续开发或对外交付，建议先决定是直接提交、继续叠加，还是先补一轮回归验证后再提交。
  - `doc/` 与部分 `Readme/` 文档目前仍显示为未跟踪；如果这些文档需要纳入正式版本，后续需要一起进入版本管理，否则容易出现“文档只在本机可见”的情况。
  - OpenSpec 已完成初始化，但当前仓库里还没有活跃的 `changes/specs` 任务单；如果后续希望把需求开发正式切进 OpenSpec 流程，下一步应补一个实际 change。

## 阶段：生图项目当前进度同步

- 阶段目标：围绕“基于 Gemini 的电商生图工作台”主项目，输出一份可直接续接的阶段性进度快照，明确当前已完成能力、工作区进行中改动和建议下一步。
- 已完成事项：
  - 确认生图项目当前对外版本为 `v0.4.0`，公开定位、功能亮点、安装方式和 GitHub Releases 入口已在 `README.md` 中整理完成。
  - 确认项目主能力已经覆盖：
    - 标准出图、提示词模式、参考图复刻、套图模式、亚马逊 A+ 图五种创作模式。
    - 历史记录、任务详情审核、通过项导出、模板中心、品牌库、设置页配置、飞书多维表格同步。
    - Windows 安装器与安全发布口径。
  - 确认 `Readme/` 目录已存在生图项目的使用说明、PRD 与版本说明，说明对外交付资料已形成基本闭环。
  - 对齐当前 Git 工作区，确认生图项目“正在进行中但尚未提交”的改动主要落在创建页和提示词链路收尾：
    - 创建页数量输入允许临时清空，并限制为 `1-10` 合法整数。
    - 服务端补充数量边界校验。
    - 原生下拉深色主题统一。
    - 新平台补充到 `拼多多`、`Temu`、`SHEIN`。
    - 尺寸/重量信息自动补全为公英双制，并接入模板、翻译、提示词优化与提示词模式调用。
    - 自动翻译漏翻增加逐字段补翻兜底。
    - 创作页头部同步摘要进一步收口，减少首屏噪音。
  - 确认这些进行中改动，与 `2026-03-13` 已登记的“数量输入/双制尺寸/翻译补救/摘要收口”等阶段记录一致，说明生图项目目前不处于方向探索期，而是处于 `v0.4.0` 发布后的细节打磨和稳定性补强阶段。
- 涉及文件：
  - `README.md`
  - `Readme/使用说明-电商AI出图工作台.md`
  - `Readme/PRD-电商AI出图工作台.md`
  - `Readme/版本说明-v0.4.0.md`
  - `components/create-job-form.tsx`
  - `app/api/generate/route.ts`
  - `app/globals.css`
  - `app/ui-ux-pro-max.css`
  - `lib/constants.ts`
  - `lib/gemini.ts`
  - `lib/templates.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前生图项目的这批收尾改动仍未形成一次正式提交；如果准备继续推功能，建议先把这一轮“创建页 + 翻译链路 + 平台扩展”整理成一个可回滚的提交点。
  - 文档和代码当前都在本机工作区里，`doc/` 与部分 `Readme/` 文件仍显示未跟踪；如果后续需要团队共享当前进度，需要把它们一并纳入版本管理。
  - 当前阶段更像“发布后稳态打磨”，下一步若要继续扩展功能，建议明确是走：
    - 生图链路继续深化（例如更多平台规则、更多单位体系、更多审核动作）。
    - 还是转入流程化治理（例如为生图项目补 OpenSpec change、补自动化回归与发布规程）。

## 阶段：V0.4.0 安装器首页 500 排查与修复

- 阶段目标：定位 GitHub 仓库中 `IMAGE-STUDIO-WINDOWS-0.4.0.exe` 安装后访问 `http://localhost:3000` 直接显示 `Internal Server Error` 的根因，并修复发布链路。
- 已完成事项：
  - 通过本地运行 `release/commerce-image-studio/server.js` 复现同类问题，并抓到服务端真实报错：
    - 首页请求返回 `500`。
    - Next.js 启动成功，但在首个请求时抛出 `Failed to load external module sharp... Could not load the "sharp" module using the win32-x64 runtime`。
    - 进一步核对 `release/commerce-image-studio/node_modules/@img/sharp-win32-x64/lib/`，确认发布目录中只有 `sharp-win32-x64.node`，缺少 `libvips-42.dll` 与 `libvips-cpp-8.17.3.dll` 两个运行时依赖。
  - 确认问题不是业务页面、数据库或安装脚本逻辑，而是发布打包链路没有把 `sharp` 的原生 DLL 一并带入绿色发布目录与安装器。
  - 在 `scripts/package-release.ps1` 中补充 `node_modules/@img/sharp-win32-x64` 的完整拷贝逻辑，确保发布目录保留 `sharp` 所需的全部运行时文件，而不只复制 `.node` 文件。
  - 重新执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\package-release.ps1 -SkipBuild -SanitizeSecrets`，确认新的 `release/commerce-image-studio/node_modules/@img/sharp-win32-x64/lib/` 已包含：
    - `sharp-win32-x64.node`
    - `libvips-42.dll`
    - `libvips-cpp-8.17.3.dll`
  - 用修复后的发布目录回归验证：
    - 使用 `cmd.exe` 拉起 `release/commerce-image-studio/server.js`。
    - 再请求 `http://127.0.0.1:3000`，返回 `STATUS=200`，首页 HTML 正常输出，不再出现 `Internal Server Error`。
  - 重新执行 `powershell -ExecutionPolicy Bypass -File .\scripts\build-inno-installer.ps1 -SkipBuild -SanitizeSecrets`，成功生成新的：
    - `release/commerce-image-studio-safe-setup.exe`
    - `release/IMAGE-STUDIO-WINDOWS-0.4.0.exe`
    说明安装器构建链已同步吃到这次修复。
- 涉及文件：
  - `scripts/package-release.ps1`
  - `scripts/build-inno-installer.ps1`
  - `release/commerce-image-studio/node_modules/@img/sharp-win32-x64/lib/`
  - `release/IMAGE-STUDIO-WINDOWS-0.4.0.exe`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前本地已生成修复后的 `IMAGE-STUDIO-WINDOWS-0.4.0.exe`，但 GitHub Releases 上的同名安装器如果尚未替换，外部用户仍会继续下载到旧的损坏版本。
  - 当前修复是基于本机 `win32-x64` 运行时补齐 `sharp` 原生 DLL；如果未来需要支持其他平台或架构，发布脚本仍需要针对对应 `@img/sharp-*` 包补齐相同策略。
  - 建议后续在发布前增加一个最小烟测：启动 release 包并请求首页，至少验证不会出现首屏 `500`。

## 阶段：v0.4.1 版本号与对外文档切换

- 阶段目标：基于安装器首屏 500 的修复，发布一个新的补丁版本 `v0.4.1`，先完成版本号、README 和交付文档口径切换。
- 已完成事项：
  - 将 `package.json` 与 `package-lock.json` 的项目版本从 `0.4.0` 升级为 `0.4.1`。
  - 更新 `README.md` 的顶部版本号、最新安装器名称，以及 “What’s New” 区块，明确 `v0.4.1` 的核心变更是修复安装器漏带 `sharp` 运行时 DLL 导致的首页 `500`。
  - 更新 `Readme/使用说明-电商AI出图工作台.md` 中的安装器名称为 `IMAGE-STUDIO-WINDOWS-0.4.1.exe`。
  - 新增 `Readme/版本说明-v0.4.1.md`，单独说明这是一个发布稳定性补丁版，重点修复安装器首屏 `Internal Server Error`，同时保留 `v0.4.0` 的完整功能基线。
  - 保留 `Readme/版本说明-v0.4.0.md` 作为上一版对外说明，方便版本追溯与回滚说明。
- 涉及文件：
  - `package.json`
  - `package-lock.json`
  - `README.md`
  - `Readme/使用说明-电商AI出图工作台.md`
  - `Readme/版本说明-v0.4.1.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前只是完成了版本口径切换；仍需要重新生成 `v0.4.1` 安装器，并确认产物名称、运行结果和首页访问都与新版本一致。
  - 如果 GitHub Releases 上后续发布 `v0.4.1`，需要同步补一份对应的 release body，避免外部用户只看到版本号变化而不知道本次是安装器修复版。

## 阶段：v0.4.1 安装器构建与本地验收

- 阶段目标：基于新的 `0.4.1` 版本号与修复后的发布脚本，生成新的 Windows 安装器，并验证发布目录首页访问正常。
- 已完成事项：
  - 执行 `powershell -ExecutionPolicy Bypass -File .\scripts\build-inno-installer.ps1 -SkipBuild -SanitizeSecrets`，成功重新生成修复后的发布目录与 Inno Setup 安装器。
  - 确认新的版本化安装器已生成：`release/IMAGE-STUDIO-WINDOWS-0.4.1.exe`。
  - 使用 `release/commerce-image-studio` 目录做本地烟测：
    - 通过 `cmd.exe` 拉起 `runtime\node.exe server.js`。
    - 请求 `http://127.0.0.1:3000` 返回 `STATUS=200`。
    - 说明 `v0.4.1` 对外交付产物已经摆脱旧版 `v0.4.0` 安装器的首屏 `500` 问题。
  - 确认当前仓库远端 `origin` 与 `gh` CLI 都可用，后续如果需要继续发 GitHub Release，当前环境具备基础工具条件。
- 涉及文件：
  - `scripts/package-release.ps1`
  - `scripts/build-inno-installer.ps1`
  - `release/IMAGE-STUDIO-WINDOWS-0.4.1.exe`
  - `release/commerce-image-studio/`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 `v0.4.1` 只是在本地工作区完成了版本切换、安装器生成和烟测；如果要让外部用户真正下载到新版本，还需要继续执行 GitHub 侧的提交、打 tag、发布或替换 Release 资产。
  - 当前工作区仍包含其他未提交改动；如果要把 `v0.4.1` 正式推送到远端，需要先确认发版提交范围，避免把不打算进入本次补丁版的在制改动一并带上。

## 阶段：GitHub 发布前授权恢复

- 阶段目标：恢复当前机器上的 GitHub CLI 登录态，为后续提交、打 tag 和发布 `v0.4.1` Release 做准备。
- 已完成事项：
  - 执行 `gh auth login -h github.com -w`，完成一次新的浏览器授权流程。
  - 使用带外沙箱校验 `gh auth status`，确认当前账号 `aEboli` 已重新处于有效登录状态。
  - 当前 `gh` 可见 token scopes 包含 `repo`，满足后续创建 GitHub Release 与上传资产的基本权限要求。
- 涉及文件：
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 GitHub 授权已经恢复，但发版前仍需先确认：是只上传新的安装器资产，还是要把本地未提交的代码与文档改动一并整理进 `v0.4.1` 源码提交后再发布。

## 阶段：v0.4.1 GitHub 发布完成

- 阶段目标：将本地 `v0.4.1` 源码提交、版本 tag 和安装器产物完整发布到 GitHub。
- 已完成事项：
  - 将当前本地源码、文档与打包脚本修复整理为提交 `70e1f25`，提交信息为 `Release v0.4.1`。
  - 创建本地 tag：`v0.4.1`。
  - 推送 `main` 到远端仓库。
  - 推送 `v0.4.1` tag 到远端仓库。
  - 使用 GitHub CLI 在仓库 `aEboli/ai-ecommerce-image-studio` 创建 Release：
    - Tag：`v0.4.1`
    - 标题：`v0.4.1`
    - Release Notes：`Readme/版本说明-v0.4.1.md`
    - 上传资产：`release/IMAGE-STUDIO-WINDOWS-0.4.1.exe`
  - 发布地址已生成：`https://github.com/aEboli/ai-ecommerce-image-studio/releases/tag/v0.4.1`
- 涉及文件：
  - `doc/进展记录.md`
  - `Readme/版本说明-v0.4.1.md`
  - `release/IMAGE-STUDIO-WINDOWS-0.4.1.exe`
- 待办事项或风险：
  - 当前 Release 已发布到新的仓库地址 `aEboli/ai-ecommerce-image-studio`；如果仍有用户从旧仓库地址进入，需要同步检查旧仓库首页、README 或 Release 页是否已足够引导跳转。
  - 当前主分支会在本条进展记录提交后比 `v0.4.1` tag 多一个“发布记录”提交，这是有意保留的运维记录，不影响 `v0.4.1` tag 对应的实际发布代码与安装器。

## 2026-03-12

## 阶段：项目摸底与进度梳理

- 阶段目标：快速确认当前“电商 AI 出图工作台”的产品定位、技术实现主链路、最近迭代重点与仍待处理的风险项，形成可续接的项目理解基线。
- 已完成事项：
  - 梳理仓库结构，确认项目已不是单页生图 Demo，而是围绕“创建任务 -> 队列执行 -> 结果审核 -> 飞书同步 -> Windows 交付”构建的本地化工作台。
  - 结合 `README.md`、`Readme/PRD-电商AI出图工作台.md`、`Readme/使用说明-电商AI出图工作台.md`、`doc/网页生图项目分析-2026-03-12.md` 与 `doc/进展记录.md`，确认当前主版本口径已进入 `v0.3.1` 阶段，最近工作重点集中在参考图复刻稳定性、详情页工作台化、飞书结构整理与创作台交互优化。
  - 结合代码确认当前主技术链路为：Next.js App Router 前端 + `node:sqlite` 本地数据库 + 进程内队列恢复机制 + Gemini 生成链路 + 飞书多维表格同步。
  - 确认任务执行链路已具备一定恢复能力：`app/layout.tsx` 启动时通过 `lib/runtime.ts` 拉起队列恢复，`lib/queue.ts` 会扫描可恢复任务，`lib/generation.ts` 只消费 `queued` 项并基于数据库状态回收任务结果。
  - 确认创建侧已形成 5 种核心模式：标准出图、提示词模式、参考图复刻、套图模式、亚马逊 A+ 模式；其中参考图复刻是最近最密集的迭代区域。
  - 额外核对到对外版本口径仍未完全收口：`package.json` 与 `Readme/版本说明-v0.3.1.md` 已是 `v0.3.1`，但 `README.md` 与 `Readme/使用说明-电商AI出图工作台.md` 里仍残留 `v0.3.0` / `IMAGE-STUDIO-WINDOWS-0.3.0.exe`。
- 涉及文件：
  - `README.md`
  - `package.json`
  - `Readme/PRD-电商AI出图工作台.md`
  - `Readme/使用说明-电商AI出图工作台.md`
  - `Readme/版本说明-v0.3.1.md`
  - `doc/网页生图项目分析-2026-03-12.md`
  - `doc/进展记录.md`
  - `app/api/generate/route.ts`
  - `app/layout.tsx`
  - `app/page.tsx`
  - `components/create-job-form.tsx`
  - `lib/db.ts`
  - `lib/feishu.ts`
  - `lib/generation.ts`
  - `lib/queue.ts`
  - `lib/runtime.ts`
- 待办事项或风险：
  - `README.md` 与部分使用说明仍残留 `v0.3.0` 口径，和当前 `v0.3.1` 状态不一致，后续如果要继续对外交付，建议优先统一版本与安装器命名。
  - 数据层当前仍依赖 `node:sqlite`，并且构建阶段仍有既有的 experimental warning 与 Windows `copyfile EINVAL` 提示，这两项属于持续性技术风险。
  - `doc/进展记录.md` 目前历史内容较多，终端中偶尔会出现编码显示不稳定；后续如果继续长期维护，建议单独做一次文档编码与结构整理。

## 阶段：网页生图项目阶段分析与优化建议沉淀

- 阶段目标：梳理当前“电商 AI 出图工作台”项目的实际完成度、近期迭代重心、当前技术风险，并输出下一阶段优化建议。
- 已完成事项：
  - 结合 `README.md`、`Readme/PRD-电商AI出图工作台.md`、`Readme/版本说明-v0.3.1.md`、核心页面与后端模块，对项目当前能力、交付状态与最近优化方向做了系统梳理。
  - 确认项目已从基础可用阶段进入可交付的 `v0.3.1` 阶段，当前重点已转向创作台体验压缩、历史记录与详情页工作台化、Gemini 尺寸档位对齐、参考图复刻稳定性与飞书协同增强。
  - 完成一次健康度校验：`npm run typecheck` 与 `npm run build` 均通过；同时确认 `next build` 末尾仍存在 Windows 环境下 standalone traced files 的 `copyfile EINVAL` 警告，以及 `node:sqlite` 实验特性警告。
  - 新增专题分析文档 `doc/网页生图项目分析-2026-03-12.md`，沉淀本次项目阶段判断与优化建议，便于后续继续跟进。
- 涉及文件：
  - `doc/进展记录.md`
  - `doc/网页生图项目分析-2026-03-12.md`
- 待办事项或风险：
  - `README.md` 仍显示 `v0.3.0` 与旧安装器名称，和 `package.json`、`Readme/版本说明-v0.3.1.md` 已存在版本不一致，建议尽快统一。
  - 当前任务队列仍为进程内内存队列，数据库仍依赖 `node:sqlite` 实验能力；如后续继续走安装器分发与多人协作，建议优先补稳定性改造与可观测性。

## 阶段：历史记录与创作台文案改名

- 阶段目标：按当前命名习惯统一前端用户可见文案，将“商品”改为“图片”，并把历史记录表格中的“平台 / 市场”调整为“生图模式 / 语言”。
- 已完成事项：
  - 调整历史记录表格列标题：`商品` 改为 `图片`，`平台` 改为 `生图模式`，`市场` 改为 `语言`。
  - 调整历史记录表格数据映射：原“平台”列改为显示任务实际 `creationMode`，原“市场”列改为仅显示 `language`。
  - 统一修改创作台、任务详情页、总览页和基础 i18n 中的用户可见“商品”文案为“图片”表述，同时保留内部字段名如 `productName` 不变，降低改动风险。
  - 完成 `npm run typecheck` 校验，确认本次纯文案与展示映射调整未引入类型错误。
- 涉及文件：
  - `components/job-table.tsx`
  - `app/history/page.tsx`
  - `components/create-job-form.tsx`
  - `components/job-details-client.tsx`
  - `app/page.tsx`
  - `lib/i18n.ts`
  - `lib/constants.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 本次只调整了前端用户可见文案与历史记录展示列，未改动数据库字段与接口字段名；如后续希望彻底统一术语，可再评估是否需要重命名 `productName` 等内部字段。
  - `Readme/` 文档中的“商品”表述本次未同步修改；如你希望对外交付口径也一起统一，建议下一步补文档层更新。

## 阶段：提示词模式关闭优化时改为原文直传

- 阶段目标：修正提示词模式的后端链路，确保关闭“自动优化为真实照片”后，自定义提示词不再经过翻译或改写，而是按用户原文直接提交给模型。
- 已完成事项：
  - 调整 `lib/generation.ts` 的提示词模式输入处理：仅在开启 `autoOptimizePrompt` 时才调用 `translateUserPromptInputs`，关闭时直接使用 `job.customPrompt` 与 `job.customNegativePrompt` 原文。
  - 调整提示词模式的生成输入装配逻辑，确保关闭优化时 `customPromptText` 直接使用用户原始提示词，不再回退到处理后的版本。
  - 同步更新创作台提示文案，明确写明关闭优化后会“直接按原文提交，不做优化、翻译或改写”。
- 涉及文件：
  - `lib/generation.ts`
  - `components/create-job-form.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前“关闭优化即原文直传”会同时跳过提示词模式下原有的翻译环节；如果后续仍希望保留“翻译但不优化”的独立能力，建议单独增加一个明确开关，而不是复用当前优化开关。

## 阶段：提示词模式拆分“翻译”和“优化”两个独立开关

- 阶段目标：将提示词模式中的处理逻辑拆成两个明确选项，让用户分别控制“是否先翻译为输出语言”和“是否继续优化为真实照片”。
- 已完成事项：
  - 新增提示词模式布尔开关 `translatePromptToOutputLanguage`，前端表单中与 `autoOptimizePrompt` 并列显示，形成两个可组合的独立选项。
  - 扩展 `lib/types.ts`、`lib/job-builder.ts`、`lib/db.ts` 的任务数据结构与数据库 schema，持久化新的翻译开关，并为历史数据迁移保留兼容默认值。
  - 调整 `lib/generation.ts` 提示词模式执行顺序为“先按需翻译，再按需优化”，支持四种组合：原文直传、仅翻译、仅优化、翻译后再优化。
  - 调整 `lib/gemini.ts` 的提示词优化函数，确保在“优化开启但翻译关闭”时保留用户原始语言，而不是隐式转成输出语言。
  - 同步更新创作台说明文案，明确两个开关的职责和组合关系。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `lib/generation.ts`
  - `lib/gemini.ts`
  - `lib/types.ts`
  - `lib/job-builder.ts`
  - `lib/db.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 历史任务由于新增了新开关字段，重试时会沿用数据库中的持久化值；如你希望把旧任务默认统一迁移成“翻译关 / 优化关”，可以后续补一轮数据修正脚本。

## 2026-03-10

### 阶段：建立全局协作规则

- 阶段目标：将阶段性进展同步、上下文提醒、使用说明与 PRD 交付要求固化到仓库中。
- 已完成事项：
  - 新增根目录 `AGENTS.md`，写入全局协作规则。
  - 建立 `doc/` 目录，并初始化 `doc/进展记录.md` 作为持续更新的进展文档。
  - 建立 `Readme/` 目录，并补充目录用途说明，约定使用说明与 PRD 的命名方式。
  - 已完成文件落地校验，确认本次新增文件已创建；仓库内同时存在其他未提交改动，本次未对其进行处理。
- 涉及文件：
  - `AGENTS.md`
  - `doc/进展记录.md`
  - `Readme/目录说明.md`
- 待办事项或风险：
  - 后续每完成一个阶段，需要继续追加更新本文件。
  - 当出现可交付给他人使用的新功能、新流程或新工具时，需要在 `Readme/` 下补充对应使用说明和 PRD。

### 阶段：README 产品落地页化并同步 GitHub

- 阶段目标：将仓库首页 README 优化为更像产品落地页的双语展示文档，并同步到 GitHub 主分支，便于外部访客快速理解 `v0.2.0` 的产品能力与发布状态。
- 已完成事项：
  - 重构根目录 `README.md` 的首页结构，调整为更适合 GitHub 展示的产品型排版。
  - 前置补充 `v0.2.0` 版本亮点，纳入三种创作模式、飞书多维表格同步、模板中心、品牌库、审核流与安全发布能力说明。
  - 保留并整理截图导览，将界面说明改写为业务场景导向的功能介绍。
  - 补充安装、运行、发布产物、FAQ 与仓库说明，并将本次 README 优化提交并推送到 GitHub。
- 涉及文件：
  - `README.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 后续如新增品牌库、模板编辑器或飞书同步状态面板，README 需要继续同步更新。
  - 若后续新增中文专属发布说明或面向客户的使用文档，可在 `Readme/` 目录中拆分更详细的交付材料。

### 阶段：参考图复刻模式必填校验修复

- 阶段目标：修复参考图复刻模式下界面已隐藏标准模式字段，但后端提交校验仍要求填写商品名，导致无法提交的问题。
- 已完成事项：
  - 排查前端与后端的提交校验链路，确认错误信息来自 `/api/generate` 的通用必填校验。
  - 修正后端逻辑，仅在标准模式下要求 `productName` 必填，参考图复刻与提示词模式不再误判。
  - 为参考图复刻模式补充任务名兜底逻辑，优先从补充说明、卖点、品牌名或文件名自动推断，避免后续详情页与文件命名出现空标题。
  - 完成 `npm run typecheck` 校验，确认改动通过类型检查。
- 涉及文件：
  - `app/api/generate/route.ts`
  - `lib/job-builder.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 建议继续实际验证一次“只上传商品图与参考图、不填写商品名”的参考图复刻提交流程，确认前后端体验完全一致。
  - 若后续希望在参考图复刻结果页中显示更友好的标题，可继续优化自动推断名称的规则。

### 阶段：GitHub 账号菜单名称修改说明

- 阶段目标：确认 GitHub 头像菜单顶部名称的字段归属，并整理可直接操作的修改入口。
- 已完成事项：
  - 根据 GitHub 官方文档确认：菜单中这类顶部标识通常涉及“用户名（username）”或“公开资料名称（profile name）”两类字段。
  - 整理用户名修改路径：`Settings` -> `Account` -> `Change username`。
  - 整理显示名称修改路径：`Settings` -> `Public profile` -> `Name`。
  - 补充用户名变更的主要影响说明，便于后续修改前做风险判断。
- 涉及文件：
  - `doc/进展记录.md`
- 待办事项或风险：
  - 若实际想修改的是企业托管账号标识，可能无法自行修改，需要由企业管理员或身份提供方处理。
  - 若后续执行用户名修改，建议同步检查本地 Git 远程地址、个人资料链接和对外文档中的旧账号引用。

### 阶段：任务详情页工作台布局优化

- 阶段目标：优化历史记录中的任务详情页面，让用户在一个工作区内更集中地查看任务信息、原图/参考图、生成图与审核操作，减少上下滚动和大图挤占空间的问题。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中新增“工作台视图”，引入选中结果预览、变体浏览器、侧栏媒体区和输入快照。
  - 为任务详情页增加 `activeItem` 焦点切换逻辑，支持在顶部主工作区聚焦单个生成项，同时保留审核、复制 prompt、对比和下载能力。
  - 在 `app/globals.css` 中补充详情页桌面端双栏布局、紧凑缩略图浏览器、粘性侧栏和更克制的预览尺寸样式。
  - 清理顶部重复的原图/参考图/对比区显示，减少同一信息在页面中的重复出现。
  - 完成 `npm run typecheck` 校验，确认改动通过类型检查。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 `127.0.0.1:3000` 上运行的是旧进程，本次新布局需要重启源码服务或正式版进程后才能在浏览器中看到。
  - 现阶段保留了页面下半部分的详细信息区作为兜底，后续可继续考虑把更多老区块折叠化或模块化，进一步缩短页面长度。

### 阶段：历史记录计数增强与详情页继续紧凑化

- 阶段目标：在历史记录列表中补充更直观的生成统计字段，并进一步缩短任务详情页首屏可视长度，让常用信息更容易完整展示。
- 已完成事项：
  - 在 `lib/db.ts` 中为任务列表查询增加 `generatedCount`、`succeededCount`、`failedCount` 聚合统计，直接从 `job_items` 计算每个任务的总生成数、成功数和失败数。
  - 在 `lib/types.ts` 中扩展 `JobRecord`，为历史记录表格提供统计字段。
  - 在 `components/job-table.tsx` 中新增“生成张数 / 成功 / 失败”列，并配套补充表格样式。
  - 继续收紧 `components/job-details-client.tsx` 与 `app/globals.css` 的详情页布局：将大块原始明细改为默认折叠，进一步降低页面纵向长度，同时将主预览区图片高度压缩到更适合首屏的尺寸。
  - 完成 `npm run typecheck` 校验，确认本次数据层与前端改动通过类型检查。
- 涉及文件：
  - `lib/db.ts`
  - `lib/types.ts`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前本地 `3000` 端口仍在运行旧进程，需要重启应用后才能在浏览器中看到最新历史页与详情页效果。
  - 如果后续觉得历史记录表列数过多，可进一步改成“任务统计胶囊”或在商品列下展示二级统计，减少表格横向压力。

### 阶段：历史记录统计胶囊 + 紧凑表格版

- 阶段目标：把历史记录页升级成“顶部统计胶囊 + 下方紧凑表格”的工作台样式，减少横向拥挤，并在单行内更完整地展示任务信息。
- 已完成事项：
  - 在 `app/history/page.tsx` 中新增顶部汇总统计区，展示任务总数、生成张数、成功张数、失败张数，并保持与筛选结果联动。
  - 在 `components/job-table.tsx` 中将原来的三列统计压缩为一列统计胶囊，同时把状态改为标签样式、时间改为日期/时间两行、商品列增加模式副信息。
  - 在 `app/globals.css` 中补充历史页汇总卡片、统计胶囊、状态标签、查看按钮与紧凑列宽样式，明显减轻表格横向压力。
  - 完成 `npm run typecheck` 校验，确认历史页与表格组件改动通过类型检查。
- 涉及文件：
  - `app/history/page.tsx`
  - `components/job-table.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前浏览器里如果仍看到旧版历史页，通常是因为本地运行的是旧进程，需要重启正式版或开发服务后再看。
  - 如果后续还希望进一步提升首屏密度，可以继续把详情页下半区的折叠块做成更细的标签页或抽屉式结构。

### 阶段：任务详情页标签页 / 抽屉化

- 阶段目标：把任务详情页下半区进一步压缩成“标签页 + 抽屉”的信息面板，让常看内容更聚焦、深层内容按需展开。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中新增详情标签状态，按“输入快照 / 参考分析 / 变体归档”切换下半区内容。
  - 将详情页下半区整合为单一“信息面板”，避免原来多个独立大面板连续堆叠。
  - 把“变体归档”改成逐项抽屉，每个变体默认收起，只在需要时展开查看图片、尺寸、审核、错误与 prompt。
  - 为输入快照和参考分析保留抽屉结构，其中常用的第一层内容默认展开，减少额外点击。
  - 在 `app/globals.css` 中补充标签按钮、抽屉摘要、焦点状态与移动端样式。
  - 完成 `npm run typecheck` 校验，确认详情页结构调整通过类型检查。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前如果浏览器中仍显示旧版详情页，需要先重启本地运行的正式版或开发服务。
  - 后续如果还想进一步缩短页面长度，可以继续把顶部工作台右侧的 prompt / 调试信息拆成小标签或二级抽屉。
### 阶段：总览页首屏工作台压缩
- 阶段目标：优化总览页，让核心统计、快捷入口、最近任务和能力概览尽量在桌面端首屏内完整展示，减少上下滑动。
- 已完成事项：
  - 重构 `app/page.tsx`，将总览页改为更紧凑的双段式工作台布局：上方为 Hero + 统计 + 快捷入口，下方为最近任务 + 能力概览。
  - 在总览页最近任务区域保留最近 3 个任务摘要，并直接显示生成总数、成功数、失败数与状态，减少跳转前的信息缺口。
  - 在 `app/globals.css` 中新增总览页紧凑样式，压缩 Hero、统计卡、快捷入口、工作流提示和最近任务卡片的高度与留白。
  - 为总览页补充响应式规则，在中等屏宽下优先改为单列，以保证紧凑布局在不同分辨率下仍可读。
  - 完成 `npm run typecheck` 校验，确认总览页结构与样式调整通过类型检查。
- 涉及文件：
  - `app/page.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前如果浏览器里还是旧版总览页，通常是本地仍在运行旧进程，需要重启正式版或开发服务后再查看。
  - 首屏是否完全无滚动还会受到用户屏幕高度、浏览器缩放和系统字体设置影响；如后续仍觉得偏高，可继续压缩最近任务卡或把能力概览改成更短的单行条目。

### 阶段：总览页二次去空白压缩
- 阶段目标：针对总览页首屏仍有大面积空白、依旧需要滚动的问题，继续收紧布局，把无效留白压到最低。
- 已完成事项：
  - 将总览页最近任务数量从 `3` 条缩减为 `2` 条，优先保留最近的关键摘要，进一步降低下半区高度。
  - 在 `app/globals.css` 中继续压缩总览页 Hero、工作流卡、统计卡、快捷入口卡、能力卡与最近任务卡的高度、内边距、间距和字体尺寸。
  - 修正总览页左侧主区域的纵向排布逻辑，取消被 `space-between` 撑开的效果，减少标题、说明、按钮和工作流之间的大片空白。
  - 移除最近任务面板的额外辅助说明行，减少下半区标题区域占高。
- 涉及文件：
  - `app/page.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 是否彻底实现“桌面端无滚动”仍取决于实际屏幕高度、浏览器缩放比例和系统字体设置；如果用户机器高度较低，可能还需要再把右侧能力卡改成更短的列表式布局。
  - 当前我能确认代码层与本地热更新方向正确，但若浏览器仍显示旧布局，需要先重启本地运行进程后再看最终效果。

### 阶段：空字段不进入提示词
- 阶段目标：避免用户未填写的品牌、卖点、补充说明等字段被自动写成 `Not specified / Not provided` 一类文案，污染最终提示词。
- 已完成事项：
  - 在 `lib/templates.ts` 中新增提示词字段规范化逻辑，仅当字段有真实内容时才输出对应的提示词行。
  - 将标准出图、提示词模式、参考图复刻、直驱复刻等模板构建链路统一改为“空字段不写入”。
  - 将默认 `general` 品类视为弱默认值，不再主动写入提示词上下文。
  - 在 `lib/gemini.ts` 中同步清理优化提示词、参考图复刻文案重写、输入翻译等链路里的占位词输出。
  - 完成 `npm run typecheck` 校验，确认本次规则调整通过类型检查。
- 涉及文件：
  - `lib/templates.ts`
  - `lib/gemini.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前仍会保留平台、国家、语言、分辨率等结构化任务参数，因为这些属于任务设定而不是空占位文本。
  - 如果后续还希望把“自动推断的商品名”也从提示词里进一步区分为“用户手填 / 系统兜底”，可以继续单独加一层来源标记。
### 阶段：创作台首屏压缩与视口适配
- 阶段目标：把创作台页面压缩成更接近单屏工作台的结构，并补上视口尺寸检测，让不同分辨率尤其是短屏笔记本下也能完整显示主体内容而不依赖整页放缩。
- 已完成事项：
  - 在 `components/create-job-form.tsx` 中新增基于 `window.innerWidth`、`window.innerHeight` 和表单顶部偏移量的视口检测逻辑，自动识别 `compact / cramped` 两档布局模式。
  - 为创作台表单增加 `--create-workspace-height` 可用高度变量，在紧凑模式下让左右两栏进入“固定工作台高度 + 内部滚动”布局，避免整页继续被拉长。
  - 将创作台主表单的“基础信息 / 参数设置”重排为 `create-primary-grid`，在宽屏下改成双栏并列，减少纵向堆叠。
  - 按视口模式动态压缩提示词、卖点、限制词、JSON 覆盖和请求头等多处 textarea 行数，降低表单纵向高度。
  - 在 `app/globals.css` 中同步收紧创作台预览区、卡片内边距、折叠面板间距、提交栏和短屏断点规则，并为 `app/create/page.tsx` 的页面说明区增加短屏高度适配样式。
  - 保持图片预览使用 `object-fit: contain`，确保不同分辨率下缩减布局时不拉伸、不失真。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `app/create/page.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前本地没有运行中的 `127.0.0.1:3000` 创作台实例，已完成类型检查但还需要在你重启应用后做一次实际视觉验收。
  - 如果重启后在某些超低高度窗口里仍有少量滚动，可继续把右侧参数区中的次级 helper 文案再裁短一档，或把模板提示卡进一步折叠成摘要态。
### 阶段：飞书字段映射模板与格式化优化
- 阶段目标：把飞书多维表格字段映射收敛成更易用的推荐模板，并在设置页提供一键填入与格式化能力，降低手写 JSON 和字段顺序混乱带来的成本。
- 已完成事项：
  - 在 `lib/feishu.ts` 中补充飞书字段映射支持键的统一顺序定义、推荐映射模板，以及映射格式化函数。
  - 将字段映射解析逻辑收紧为只接受系统实际支持的键，自动忽略未知字段，避免无效配置混入保存结果。
  - 在 `app/api/settings/route.ts` 中加入保存时的飞书映射规范化处理，保存后自动按推荐顺序输出 JSON。
  - 重写 `components/settings-form.tsx` 的设置页文案与飞书映射输入区，新增“填入推荐模板”和“格式化映射”按钮，并补充可用字段说明。
  - 将 `lib/server-config.ts` 的默认飞书映射替换为推荐模板，确保新环境默认就是更合理的字段布局。
  - 完成 `npm run typecheck` 校验，确认本次飞书字段映射与设置页改动通过类型检查。
- 涉及文件：
  - `lib/feishu.ts`
  - `lib/server-config.ts`
  - `app/api/settings/route.ts`
  - `components/settings-form.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前推荐模板偏“运营落表优先”，如果你后续更想做审核流或投放流，也可以再拆一版“精简模板 / 全量模板”双模板切换。
  - 设置页改动已完成代码和类型层验证，如需确认实际呈现，重启本地应用后进入设置页查看新按钮和模板显示即可。

### 阶段：飞书多维表格改为单任务单行、多图同格
- 阶段目标：将飞书同步从“每张成功图片单独建一行”改为“每个任务只保留一行，成功图片持续追加到同一图片单元格”，同时补齐任务级摘要字段。
- 已完成事项：
  - 在 `lib/feishu.ts` 中新增任务级飞书同步入口 `syncJobToFeishu`，支持按 `jobId` 查找已有记录、复用记录并向同一图片字段追加多个 `file_token`。
  - 将飞书写入字段从单图语义调整为任务级语义：标题不再附带 `#1 / #2`，并补充图片统计、比例汇总、分辨率汇总、尺寸汇总、生成统计等摘要字段。
  - 保持实时同步策略不变：首张成功图创建记录，后续成功图更新同一行；失败项不上传图片，但会在后续汇总同步时更新统计文本。
  - 在 `lib/db.ts` 与 `lib/types.ts` 中落库任务级飞书同步状态，记录 `feishuRecordId` 与已上传 `fileToken` 列表，避免重复建行。
  - 在 `lib/generation.ts` 中把生成链路切换到“先写本地结果，再做飞书聚合同步”的顺序，并在成功、失败、任务结束三个阶段都触发任务级汇总刷新。
  - 完成 `npm run typecheck` 校验，确认飞书同步链路与任务生成链路的类型定义一致。
- 涉及文件：
  - `lib/feishu.ts`
  - `lib/generation.ts`
  - `lib/db.ts`
  - `lib/types.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 新逻辑只影响修改上线后的新任务，旧的飞书历史记录不会自动迁移为“单任务单行”结构。
  - 如果飞书表字段映射中未配置新增摘要字段，系统会继续同步图片与现有共享字段，但表内不会自动出现新的摘要列。

### 阶段：创作台新增亚马逊 A+ 图创作模式
- 阶段目标：在创作台中新增面向 Amazon A+ 详情页的固定套图模式，让用户上传主图后，按国家、语言、比例、分辨率和数量一次生成完整的 A+ 模块组合。
- 已完成事项：
  - 在 `lib/types.ts` 中扩展 `creationMode`，新增 `amazon-a-plus`，并补充 A+ 专用图位类型 `feature-overview`、`multi-scene`、`size-spec`、`culture-value`。
  - 在 `lib/constants.ts` 与 `lib/templates.ts` 中补齐 A+ 模块的标签、描述和默认提示词策略，覆盖主视觉海报、核心卖点解析、多场景应用、产品细节、尺寸标注与文化价值六类模块。
  - 在 `app/api/generate/route.ts` 中新增 Amazon A+ 模式预处理逻辑：自动锁定平台为 Amazon，固定展开六个 A+ 图位，沿用比例 / 分辨率 / 数量作为可选参数。
  - 在 `components/create-job-form.tsx` 中新增 Amazon A+ 模式入口和界面说明，并把创作台表单改成 A+ 模式下的紧凑工作流：主图上传 + 国家语言 + 固定模块预览 + 数量控制。
  - 在 `components/job-table.tsx`、`components/job-details-client.tsx`、`lib/feishu.ts` 中补充 Amazon A+ 模式标签显示，保证历史记录、详情页和飞书同步描述一致。
  - 完成 `npm run typecheck` 校验，确认 A+ 模式在创建页、任务类型与生成链路上的类型定义一致。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `app/api/generate/route.ts`
  - `lib/types.ts`
  - `lib/constants.ts`
  - `lib/templates.ts`
  - `lib/gemini.ts`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `lib/feishu.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 Amazon A+ 模式优先做了“固定六模块”的主流程，后续如需更细的模块级文案输入，可再补专门的 A+ 卖点编辑面板。
  - A+ 模式目前会锁定 Amazon 平台，但国家、语言、比例、分辨率和数量仍可调；上线前建议实际生成一轮验证六模块风格是否符合你预期。

### 阶段：创作台恢复套图模式并与亚马逊 A+ 模式分流
- 阶段目标：在创作台中同时保留“套图模式”和“亚马逊 A+ 模式”，恢复通用套图六模块工作流，明确两种固定套图模式的职责边界，并补齐新图位在模板、详情页和飞书中的展示。
- 已完成事项：
  - 在 `lib/types.ts` 中补充 `main-image`、`lifestyle`、`material-craft` 三个新图位类型，保留 `suite` 与 `amazon-a-plus` 两种创作模式并存。
  - 在 `lib/constants.ts`、`lib/templates.ts` 中补齐套图模式新图位的标签、说明和默认模板种子，形成通用套图六模块：主图、生活方式、卖点总览、场景图、材质工艺、尺寸参数。
  - 在 `lib/db.ts` 中把默认模板补种逻辑从“仅空表初始化”升级为“缺哪种默认模板就补哪种”，保证老数据库也能拿到新图位的默认模板。
  - 在 `app/api/generate/route.ts` 中修正 `suite` 模式固定图位集合，并将 `suite` 的必填校验调整为品类、卖点、材质、尺寸大小；同时取消 `suite` 对商品名的强制要求。
  - 在 `lib/job-builder.ts` 中为 `suite` 与 `amazon-a-plus` 增加更合理的任务名兜底逻辑，避免继续落回提示词模式的命名策略。
  - 在 `components/create-job-form.tsx` 中恢复 `套图模式` 独立入口，新增套图模式固定模块说明、材质/尺寸字段与“套图数量”文案；同时保留 `亚马逊 A+` 独立入口，并将其数量语义保持为“每个模块生成数量”。
  - 在 `components/job-details-client.tsx` 与 `lib/feishu.ts` 中补齐新图位的人类可读标签，避免详情页和飞书摘要继续暴露内部英文 key。
  - 完成 `npm run typecheck` 校验，确认本次模式分流、模板补种与前端表单改动通过类型检查。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `app/api/generate/route.ts`
  - `components/job-details-client.tsx`
  - `lib/db.ts`
  - `lib/job-builder.ts`
  - `lib/feishu.ts`
  - `lib/constants.ts`
  - `lib/templates.ts`
  - `lib/types.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前实现已恢复两种固定套图模式的展开与校验，但还需要你实际创建一次 `套图模式` 与 `亚马逊 A+` 任务，确认生成队列中 6 模块展开顺序、数量语义和结果页标题完全符合预期。
  - 套图模式目前沿用现有品类选择器，默认值仍是 `general / 泛品`；如果后续你希望“必须主动选择品类”而不是允许默认值通过，需要再补一层前端显式提醒或空值占位项。

## 2026-03-11

### 阶段：套图模式飞书大图同步稳定性修复

- 阶段目标：排查套图模式在 30MB 以上图片同步飞书多维表格时失败的问题，并提升上传链路对偶发网络抖动的容错能力。
- 已完成事项：
  - 复现并拆分验证了飞书链路，确认 37MB PNG 的分片上传本身可成功，且多张大图写入同一条多维表格记录也可成功，问题更接近于批量同步过程中的偶发 `fetch failed`。
  - 在 `lib/feishu.ts` 中为 `upload_all`、`upload_prepare`、`upload_part`、`upload_finish` 增加统一的可恢复重试逻辑，并对分片上传增加“整次会话重建”兜底，避免单次网络失败直接导致大图同步中断。
  - 为飞书记录的创建、更新、读取和按 `jobId` 查找同样补上重试保护，让“单任务单行、多图同格”的后续更新更稳定。
  - 完成 `npm run typecheck` 校验，确认本次修复未引入类型错误。
- 涉及文件：
  - `lib/feishu.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 需要在本地重启应用后，重新用套图模式生成一组包含 30MB 以上 PNG 的任务，确认飞书不再出现 `fetch failed` 警告。
  - 这次修复面向新发生的同步流程；已经失败的历史任务不会自动补传，如需补齐可重新生成或后续补一个“重新同步飞书”入口。

### 阶段：任务详情页新增飞书重同步入口

- 阶段目标：为已生成完成但曾经飞书同步失败的历史任务提供补传入口，无需整单重跑即可重新上传当前任务的成功图片并刷新飞书记录。
- 已完成事项：
  - 在 `lib/feishu.ts` 中新增任务级 `rebuildJobFeishuSync`，会重新读取当前任务的所有成功生成图，重新上传并重建该任务在飞书中的图片字段。
  - 抽出飞书记录状态解析与任务级 upsert 逻辑，让增量同步和重建同步共用同一套记录查找 / 更新链路。
  - 新增 `app/api/jobs/[id]/feishu-sync/route.ts`，提供任务级飞书重同步接口，并在成功后回写新的 `recordId / fileTokens`，同时清理或更新对应 item 的飞书 warning。
  - 在 `components/job-details-client.tsx` 的任务详情页头部加入“重新同步飞书”按钮，支持直接对当前任务补传，并在前端显示成功/失败反馈。
  - 完成 `npm run typecheck` 校验，确认本次接口与前端联动改动通过类型检查。
- 涉及文件：
  - `lib/feishu.ts`
  - `app/api/jobs/[id]/feishu-sync/route.ts`
  - `components/job-details-client.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前重同步会重新上传该任务下所有成功生成图，并重建飞书图片字段；它不会做 token 级去重，但会覆盖当前任务在飞书这一行的图片集合。
  - 如需更进一步的运营体验，后续可以再补一个“仅补传失败图片”或“批量重同步多个任务”的入口。

### 阶段：启动失败修复与 Feishu 字段映射依赖拆分
- 阶段目标：修复应用无法启动的问题，排除客户端构建错误地引用服务端 Feishu 模块导致的 `node:fs` 构建失败。
- 已完成事项：
  - 新增 `lib/feishu-field-mapping.ts`，将 Feishu 字段映射的纯工具函数独立出来，避免客户端和轻量接口再依赖服务端版 `lib/feishu.ts`。
  - 更新 `components/settings-form.tsx`、`app/api/settings/route.ts`、`app/api/settings/test-feishu/route.ts`、`lib/server-config.ts` 的导入链路，改为使用新的纯映射模块。
  - 完成 `npm run typecheck` 与 `npm run build` 校验，构建通过。
  - 完成一次本地生产启动验证：通过 `npm start -- --hostname 127.0.0.1 --port 3021` 拉起服务，并成功请求首页返回 `200 OK`。
- 涉及文件：
  - `lib/feishu-field-mapping.ts`
  - `components/settings-form.tsx`
  - `app/api/settings/route.ts`
  - `app/api/settings/test-feishu/route.ts`
  - `lib/server-config.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前修复已覆盖启动与构建链路，但你本地若仍开着旧进程，需要先关闭后重新启动，才能看到修复后的版本。
  - `doc/进展记录.md` 现有内容存在一部分历史编码异常，后续建议单独整理一次文档编码，避免继续出现乱码段落。

### 阶段：任务详情页补充实际返回大小（MB）展示
- 阶段目标：在任务详情页的尺寸与比例参数区增加“实际返回大小（MB）”信息，方便快速判断生成图片体积与飞书同步风险。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中新增 `formatSizeMb` 格式化函数，并补充中英文文案 `actualFileSize`。
  - 在主工作区右侧参数区和下方变体抽屉参数区同时新增“实际返回大小”卡片，直接展示生成图的 `sizeBytes` 换算后的 MB 值。
  - 在 `app/globals.css` 中为新增卡片加入跨两列样式，保证它显示在尺寸/比例卡片下方，布局更清晰。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前展示基于 `generatedAsset.sizeBytes`，如果某个变体尚未生成纯图，则该字段会显示为“未知”。
  - 如后续你希望同时展示文案图大小，可以再补一个“文案图大小”字段，但这次先保持右侧参数区简洁。

### 阶段：套图/A+字段按图位隔离并补充自动翻译与双单位尺寸提示
- 阶段目标：让创作台输入的卖点、材质、尺寸信息在生成前先翻译到目标语言，并按图位精准分发，避免材质和尺寸污染其它图片；同时让尺寸图明确按目标市场输出公制/英制双格式。
- 已完成事项：
  - 新增 `lib/creative-fields.ts`，把 `sourceDescription` 中的通用补充说明、材质信息、尺寸信息拆分为独立字段，并在建任务时继续兼容原有存储格式。
  - 在 `lib/gemini.ts` 中扩展创意输入翻译逻辑，新增对 `materialInfo` 和 `sizeInfo` 的单独翻译，保证这些字段也会先转成目标语言后再参与生成。
  - 在 `lib/generation.ts` 中改为先拆分再翻译再组装的流程，生成时分别把 `sourceDescription / materialInfo / sizeInfo` 传给模板层，而不是再把材质和尺寸混进统一备注字段。
  - 在 `lib/templates.ts` 中为不同图位增加硬隔离规则：卖点仅进入卖点图（及痛点图），材质仅进入材质工艺图，尺寸/重量仅进入尺寸参数图；同时为尺寸图补充双单位展示指令与常见单位自动换算参考。
  - 在 `components/job-details-client.tsx` 中补充输入快照显示，单独展示翻译后的材质与尺寸/重量字段，便于回看模型实际使用的内容。
  - 完成 `npm run typecheck` 与 `npm run build` 校验，确认改动未破坏启动与构建链路。
- 涉及文件：
  - `lib/creative-fields.ts`
  - `lib/job-builder.ts`
  - `lib/types.ts`
  - `lib/gemini.ts`
  - `lib/generation.ts`
  - `lib/templates.ts`
  - `components/job-details-client.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前“双格式”主要通过提示词约束和常见单位自动换算参考实现，足以覆盖 `cm / mm / m / in / ft / g / kg / oz / lb` 等常见输入；若后续需要对更复杂的包装规格文本做强校验，可再补更完整的结构化解析器。
  - 旧历史任务不会回写新的分字段翻译结果；新提交的套图和 A+ 任务会按新规则生效。 

### 阶段：任务详情页将生成项浏览移至结果工作台上方
- 阶段目标：优化任务详情页的浏览顺序，先选中生成项，再在下方结果工作台查看大图和详细信息，减少来回视线跳转。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中调整主工作区区块顺序，将“生成项浏览”面板移动到“结果工作台”上方。
  - 同步更新中英文提示文案，使其描述与新布局一致：先在上方浏览器中切换生成项，下方工作台同步展示完整内容。
  - 保持现有对比、审核、下载与详细参数功能不变，仅调整交互顺序。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前只调整了区块顺序，没有改动缩略卡尺寸；如果你后续希望上方浏览器再更紧凑一点，可以继续压缩卡片高度或改成横向滚动条。
## 阶段：Gemini 3.1 Flash Image 宽高比与尺寸档位对齐（2026-03-11）
- 阶段目标：根据 Gemini 3.1 Flash Image Preview 官方文档，重设创作台宽高比选项，并修复结果页把尺寸档位误判为固定像素导致的错误尺寸警告。
- 已完成事项：
  - 按官方支持范围更新宽高比选项为 1:1、3:2、2:3、3:4、4:1、4:3、4:5、5:4、8:1、9:16、16:9、21:9。
  - 移除创作台分辨率选项里的 512px，仅保留官方支持的 1K、2K、4K。
  - 新增共享尺寸策略模块，结果页“请求尺寸”改为显示 Gemini 尺寸档位而不是本地推算像素。
  - 生成链路改为仅在实际返回宽高比与请求比例不一致时才告警，不再因为 Gemini 返回原生像素尺寸不同而误报。
  - 详情页实际比例识别同步支持新的官方比例集合。
  - 已验证 npm run typecheck 与 npm run build 通过。
- 涉及文件：
  - lib/constants.ts
  - lib/utils.ts
  - lib/image-size-policy.ts
  - lib/generation.ts
  - components/job-details-client.tsx
  - components/create-job-form.tsx
- 待办事项 / 风险：
  - 当前详情页仍保留旧的本地尺寸字段用于历史兼容，后续如果要进一步减少歧义，可以把更多页面统一改成“尺寸档位 + 实际像素”的展示方式。
  - next build 末尾仍有 Windows 环境下 standalone traced files 的 copyfile 警告，与本次功能改动无直接关系，可后续单独处理。
## 阶段：Gemini 3.1 Flash Image 最新官方支持同步（2026-03-11）
- 阶段目标：根据最新 ai.google.dev 官方文档，补齐 Gemini 3.1 Flash Image Preview 新增支持的 0.5K 分辨率和新增宽高比，并保持结果页尺寸警告逻辑与模型“尺寸档位”语义一致。
- 已完成事项：
  - 将创作台宽高比选项更新为最新官方支持集合：1:1、1:4、1:8、2:3、3:2、3:4、4:1、4:3、4:5、5:4、8:1、9:16、16:9、21:9、9:21。
  - 恢复并对齐分辨率选项为 0.5K、1K、2K、4K。
  - 保留对旧数据 `512px` 的兼容读取，并在调用 Gemini 3 模型时自动转换为官方值 `0.5K`。
  - 参考图复刻模式不再强制剔除低分辨率档位，最新官方支持范围内的尺寸档位可直接使用。
  - 维持“请求尺寸按档位解释”的修复：结果页请求尺寸显示为 bucket，避免将 Gemini 3.1 Flash Image 的返回像素误判为尺寸异常。
  - 已验证 npm run typecheck 与 npm run build 通过。
- 涉及文件：
  - lib/constants.ts
  - lib/utils.ts
  - lib/image-size-policy.ts
  - lib/gemini.ts
  - components/create-job-form.tsx
  - app/api/generate/route.ts
- 待办事项 / 风险：
  - 目前生成链路仍残留一小段旧尺寸 helper 代码，仅作为历史兼容保留，后续可做一次纯清理。
  - next build 末尾仍有 Windows standalone traced files 的 copyfile 警告，与本次功能改动无直接关系，可单独处理。
## 阶段：历史记录缩略图预览列（2026-03-11）
- 阶段目标：将历史记录页中原 SKU 列替换为生成图缩略图预览，最多显示 8 张，并支持鼠标悬停放大查看。
- 已完成事项：
  - 为任务列表补充任务级预览图数据，按任务聚合最多 8 张生成图缩略图。
  - 历史记录表将原 SKU 列替换为预览列，8 张及以下全部显示，超过 8 张时仅展示前 8 张，并在第 8 张上显示剩余数量标记。
  - 新增 hover 放大预览：鼠标移入缩略图时显示固定浮层大图，移出后自动关闭。
  - 调整历史记录表列宽与样式，兼容新的缩略图宫格布局。
  - 已验证 npm run typecheck 与 npm run build 通过。
- 涉及文件：
  - lib/types.ts
  - lib/db.ts
  - components/job-table.tsx
  - app/globals.css
- 待办事项 / 风险：
  - 当前 hover 放大为桌面端交互，触屏设备仍以点击进入详情页为主；如后续需要移动端预览，可再补点击大图弹层。
  - next build 末尾仍有 Windows standalone traced files 的 copyfile 警告，与本次历史页改动无直接关系。
## 阶段：历史记录缩略图直达详情与大图悬浮预览（2026-03-11）
- 阶段目标：让历史记录页的缩略图既能悬浮放大查看，又能点击后直接跳转到任务详情页对应生成项。
- 已完成事项：
  - 任务预览数据新增关联的 jobItemId 与 imageType，支持从缩略图直接定位到详情页对应生成项。
  - 任务详情页支持读取 `?item=<jobItemId>` 查询参数，打开页面时自动聚焦到指定生成项。
  - 历史记录缩略图改为可点击链接，点击后跳转到对应任务详情页并直接选中目标生成项。
  - hover 放大预览改为更大尺寸的固定悬浮面板，并在面板侧边显示对应图型名称。
  - 已验证 npm run typecheck 与 npm run build 通过。
- 涉及文件：
  - lib/types.ts
  - lib/db.ts
  - app/jobs/[id]/page.tsx
  - components/job-details-client.tsx
  - components/job-table.tsx
  - app/globals.css
- 待办事项 / 风险：
  - 当前 hover 预览仍为桌面端交互，触屏场景未做单独弹层逻辑。
  - 如果后续希望在 hover 面板中展示更多信息（如比例、分辨率、序号），可以在当前数据结构基础上继续扩展。
## 阶段目标
优化历史记录页缩略图悬浮预览，让大图与缩略图列贴边展开，并把比例/分辨率信息更自然地融入预览面板。

## 已完成事项
- 历史记录缩略图悬浮预览改为贴边展开，不再与缩略图区留出明显空隙。
- 悬浮预览按图片真实宽高比计算大图尺寸，避免不同图比例下预览框观感失衡。
- 在预览大图左上角加入比例、分辨率信息胶囊，提高信息可读性与整体美观度。
- 右侧信息栏保留图型名，并补充比例、分辨率字段，点击缩略图仍可直达详情页对应生成项。
- 同步修正历史记录预览资产数据，补齐 ratio 和 resolutionLabel 字段供悬浮面板使用。

## 涉及文件
- lib/types.ts
- lib/db.ts
- components/job-table.tsx
- app/globals.css

## 待办事项 / 风险
- 当前悬浮预览已贴边和放大，但仍建议在真实运行页面中再目测一轮，确认不同浏览器缩放比例下不会出现遮挡。
- Next.js build 仍会出现既有的 traced files copyfile EINVAL 警告，和本次 UI 改动无关。
## 阶段目标
继续修正历史记录页缩略图悬浮预览的垂直定位，避免鼠标悬停靠近底部时大图整体上浮。

## 已完成事项
- 调整悬浮预览的垂直布局策略，优先贴着当前缩略图所在行展开，而不是为了容纳固定高度整体上移。
- 底部空间不足时允许悬浮预览缩小，优先保证与当前鼠标所在缩略图保持贴边关系。
- 保持比例、分辨率胶囊和图型信息展示逻辑不变。

## 涉及文件
- components/job-table.tsx

## 待办事项 / 风险
- 建议在真实页面中再检查最底部几行缩略图的悬浮效果，确认缩小后的预览尺寸仍满足可读性。
## 阶段目标
将创作台“提交任务”从底部提交条挪到主工作区顶部空白区域，减少底部大面积占位并提升首屏操作效率。

## 已完成事项
- 将创作台底部提交区改为顶部横向操作条，位置移动到主区上方、参数与基础信息卡片之前。
- 保留原有请求次数、计算方式、提示信息和错误提示，不再单独占据页面底部整块区域。
- 顶部操作条在窄屏下会自动改为纵向堆叠，避免按钮和说明挤压。

## 涉及文件
- components/create-job-form.tsx
- app/globals.css

## 待办事项 / 风险
- 建议在真实页面中检查顶部操作条与下方卡片的视觉间距是否还需要再压缩一档。
- 旧的 create-submit-bar 样式仍保留为兼容类，目前新布局已不再使用底部提交条。
## 阶段目标
优化创作台“生成参数”排版，按固定网格重排图片类型、比例和分辨率按钮，提升首屏可读性。

## 已完成事项
- 图片类型改为桌面端 7 列排布，形成 7×2 的固定视觉网格。
- 比例改为桌面端 5 列排布，形成 5×3 的固定视觉网格。
- 分辨率改为整行等宽按钮组，激活态使用更明显的面板式高亮。
- 为不同宽度窗口补充响应式降列规则，避免窄屏时按钮挤压变形。

## 涉及文件
- components/create-job-form.tsx
- app/globals.css

## 待办事项 / 风险
- 建议在真实页面再看一轮 1240px 左右宽度下的按钮换行效果，确认文字长度没有出现局部拥挤。
- 当前仅调整了生成参数区块，创作模式与复刻强度等其他 chip 区未改动。
## 阶段目标
继续优化创作台生成参数区的视觉密度，让图片类型更紧凑、比例标签更短、分辨率更像分段切换控件。

## 已完成事项
- 图片类型卡片压缩为更矮的双行卡片，隐藏原生勾选框，减少视觉噪音。
- 比例按钮保留 5 列排布，并将中文标签缩短为更紧凑的简称。
- 分辨率按钮改成整排 segmented control 风格，激活态更清晰。
- 三类参数按钮统一改为更像卡片按钮的交互表现，保持响应式降列规则不变。

## 涉及文件
- lib/constants.ts
- app/globals.css

## 待办事项 / 风险
- 建议在真实页面中再看一轮 7 列图片类型在长文案情况下的断行效果，确认两行截断是否符合预期。
- 当前仅调整中文比例标签，英文标签保持原样。
## 阶段目标
统一中文显示口径，确保前端对外文案使用简体中文，不使用繁体中文表述。

## 已完成事项
- 复查 app、components、lib 下用户可见文案，未发现明确繁体中文残留。
- 将语言切换入口中的中文选项从“中文”明确调整为“简体中文”，避免口径含糊。

## 涉及文件
- lib/constants.ts

## 待办事项 / 风险
- 当前已确认主要前端文案为简体中文；如果后续新增模块引入第三方文案或复制旧文案，仍需沿用简体中文标准。
## 阶段目标
在设置页 Gemini/中转 与 飞书同步 两个区块右上角补充接入说明，帮助用户快速完成配置。

## 已完成事项
- 在 Gemini / 中转设置区块右上角新增“接入说明”卡片，说明官方 Gemini 与兼容中转的填写方式及测试顺序。
- 在飞书多维表格同步区块右上角新增“接入说明”卡片，说明 App ID / Secret、App Token / Table ID 与字段映射的最小配置要求。
- 为两块说明卡补充响应式布局，窄屏下会自动落到标题下方，不会挤压表单。

## 涉及文件
- components/settings-form.tsx
- app/globals.css

## 待办事项 / 风险
- 当前说明为简洁版接入指南；如果后续要交付给外部使用者，可再补一版详细使用说明到 Readme/ 目录。

## 阶段：提示词简体中文统一与空字段清理（2026-03-11）
- 阶段目标：统一提示词中的中文输出约束为简体中文，并确保未输入的数据字段不会以默认占位或空值形式发送给模型。
- 已完成事项：
  - 在 Gemini 翻译、提示词优化、参考图分析与参考图文案生成链路中，补充“如输出中文，仅使用简体中文，不使用繁体中文”的统一约束。
  - 清理图片生成、文案生成、提示词模式、参考图复刻等 prompt builder 中的默认 Restrictions 占位文案，改为仅在用户真实填写限制词时才发送。
  - 清理参考图相关 prompt 中的 `N/A`、`not applicable`、`(leave empty...)` 等空值占位，避免把缺失字段伪装成真实输入发给模型。
  - 保持现有按图型分发字段的逻辑不变：卖点、材质、尺寸等仍然只在对应图型里进入 prompt。
  - 已验证 `npm run typecheck` 通过。
- 涉及文件：
  - lib/gemini.ts
  - lib/templates.ts
- 待办事项 / 风险：
  - 旧历史任务中已落库的 prompt 文本不会自动回写，新的“简体中文 only / 空字段不发送”规则从新任务开始生效。
  - 建议在真实生成任务里再抽测一轮：中文输出、参考图复刻、提示词模式三条链路，确认文案与图片结果符合预期。

## 阶段：提示词模式新增真实照片优化选项（2026-03-11）
- 阶段目标：在提示词模式中新增显式的“自动优化”开关，并将优化方向收敛为更真实的商业摄影风格，提升生成结果的照片真实感。
- 已完成事项：
  - 在提示词模式表单中新增“自动优化为真实照片”开关，用户可直接控制是否启用 prompt 自动优化。
  - 将开关说明改为真实摄影导向，明确强调自然光线、镜头感、材质细节、真实阴影和商业摄影质感。
  - 修复提示词模式下的状态重置问题：此前进入 prompt 模式时 `autoOptimizePrompt` 会被 `useEffect` 自动重置为 `false`，现已移除这段错误逻辑。
  - 调整 `optimizeUserImagePrompt` 的优化策略，明确要求输出更偏真实照片、避免插画感、CGI 感、塑料质感和不真实布景。
  - 已验证 `npm run typecheck` 通过。
- 涉及文件：
  - components/create-job-form.tsx
  - lib/gemini.ts
- 待办事项 / 风险：
  - 当前该选项只作用于“提示词模式”，标准模式、套图模式、A+ 模式仍沿用原有文案与图片生成链路。
  - 建议在真实任务中对比开启/关闭该开关各生成 1 组，观察结果是否更接近真实商业摄影预期。

## 阶段：历史记录缩略图改为 medium-zoom 点击放大（2026-03-11）
- 阶段目标：将历史记录页预览列从自定义 hover 贴边大图切换为 `medium-zoom` 点击放大模式，减少漂移和遮挡问题，并明确“看图”和“看详情”的入口职责。
- 已完成事项：
  - 引入 `medium-zoom` 依赖，历史记录页缩略图改为点击后居中遮罩放大查看。
  - 移除原有 `hoverPreview` 状态、自定义贴边悬浮大图面板，以及图型/比例/分辨率 hover 侧栏逻辑。
  - 保留缩略图网格、圆角、边框和最多展示 8 张图片的规则；超过 8 张时继续显示 `+N` 覆盖层。
  - 将缩略图点击行为与详情跳转拆分：缩略图只负责放大，右侧“查看”按钮继续作为唯一详情入口。
  - 将放大样式回归为“完整显示优先”的朴素模式：移除额外圆角、阴影和背景模糊，强制放大图按 `contain` 完整显示，避免裁切或奇怪的视觉效果。
  - 将 `medium-zoom` 源码直接 vendoring 到项目本地 `lib/vendor/`，附带 MIT 许可证与类型声明，并移除 `package.json` 中对外部 `medium-zoom` 包的运行时依赖。
  - 已验证 `npm run typecheck` 通过。
- 涉及文件：
  - package.json
  - package-lock.json
  - components/job-table.tsx
  - app/globals.css
  - lib/vendor/medium-zoom.js
  - lib/vendor/medium-zoom.d.ts
  - lib/vendor/medium-zoom.LICENSE.txt
- 待办事项 / 风险：
  - 建议在真实页面中再确认不同宽高比图片（1:1、3:4、16:9、9:16）的放大观感是否符合预期。
  - 移动端当前以基础点击放大为主，未额外补充手势层级定制。

## 阶段：对外交付文档整理与敏感信息收口（2026-03-11）
- 阶段目标：整理一套适合对外发布和交付的文档，去掉安装器生成等内部研发细节，并补充正式 PRD 与交付版使用说明。
- 已完成事项：
  - 更新主 README，将版本号预备调整为 `v0.3.0` 展示口径，并移除安装器生成命令与源码运行等内部开发侧说明。
  - 调整根目录 `使用说明-简体中文.md`，删除原第十一、十三、十四节，保留面向最终使用者的内容。
  - 在 `Readme/` 目录新增交付版使用说明 `使用说明-电商AI出图工作台.md`。
  - 在 `Readme/` 目录新增正式 PRD `PRD-电商AI出图工作台.md`，且不包含版本迭代路线和内部打包流程。
- 涉及文件：
  - README.md
  - 使用说明-简体中文.md
  - Readme/使用说明-电商AI出图工作台.md
  - Readme/PRD-电商AI出图工作台.md
- 待办事项 / 风险：
  - 还需要把版本号同步到 `package.json` 与安装器产物，并为 GitHub Release 准备新的版本说明。
  - 当前机器未登录 `gh`，GitHub Release 的直接上传可能会受认证限制，需要在实际发布阶段再确认。

## 阶段：v0.3.0 发布校验与安装器产物生成（2026-03-11）
- 阶段目标：完成 `v0.3.0` 的发布前校验，生成新的版本化安装器产物，并为 GitHub 更新与 Release 发布做准备。
- 已完成事项：
  - 运行 `npm run typecheck`，类型检查通过。
  - 运行 `npm run build`，生产构建通过。
  - 运行 `npm run package:installer:exe:safe`，生成新的安全安装器产物。
  - 产出新的版本化安装器：`release/IMAGE-STUDIO-WINDOWS-0.3.0.exe`。
  - 已将当前代码推送到 GitHub 主仓库，并创建 `v0.3.0` 标签。
  - 已在 GitHub 发布 `v0.3.0` Release，并上传 `IMAGE-STUDIO-WINDOWS-0.3.0.exe` 安装器资产。
- 涉及文件：
  - package.json
  - package-lock.json
  - release/IMAGE-STUDIO-WINDOWS-0.3.0.exe
- 待办事项 / 风险：
  - Next.js 生产构建仍会出现既有的 `traced files copyfile EINVAL` 警告，但本次构建与安装器生成均已成功，不影响当前发布。
## 阶段：4K 档位低返图与隐藏自动切语言修复（2026-03-11）
- 阶段目标：修复选择 4K 却被悄悄返回 1K 的误导性成功问题，并修复套图、亚马逊 A+、提示词模式在切换国家时被隐藏逻辑自动改成日语输出的问题。
- 已完成事项：
  - 重建 `lib/image-size-policy.ts`，新增尺寸档位命中判断，并将尺寸警告从“只看比例”升级为“比例 + 尺寸档位”联合判断。
  - 在生成链路中加入 Gemini 尺寸档位重试逻辑：当请求 `0.5K / 1K / 2K / 4K` 但实际返回明显低于所选档位时，自动重试；连续多次仍未命中则直接按失败处理，不再把低档位结果当作成功的 4K 图。
  - 将尺寸警告按界面语言输出，避免中文界面继续显示英文 bucket 警告。
  - 修复创作台国家切换逻辑：只有标准模式且用户显式开启“跟随国家自动切换语言”时才自动改语言；套图、亚马逊 A+、提示词模式不再被隐藏状态偷偷切到日语等目标语言。
  - 已验证 `npm run typecheck` 与 `npm run build` 通过。
- 涉及文件：
  - `lib/image-size-policy.ts`
  - `lib/generation.ts`
  - `components/create-job-form.tsx`
- 待办事项 / 风险：
  - 旧任务中已经落库的提示词与历史结果不会被回写，需用新任务重新验证。
  - 如果中转站本身持续无视 Gemini 3.x 的尺寸档位，本次修复会把该类结果明确标成失败，而不是继续显示成“4K 成功”。
## 阶段：提示词模式市场参数改为可选开关（2026-03-11）
- 阶段目标：将提示词模式下的国家、输出语言、平台从默认展示改为可选参数，并移动到生成参数区下方，减轻主流程表单负担。
- 已完成事项：
  - 为提示词模式新增“自定义目标市场参数（可选）”开关，默认关闭，只有勾选后才展开国家、输出语言和平台字段。
  - 将这组三个参数从参数设置顶部移到生成参数区下方，避免打断提示词、比例、分辨率和数量的主路径。
  - 为新开关补充中英文说明文案，并将开关状态纳入本地草稿保存与恢复逻辑。
  - 保持后端提交结构不变，未展开时仍使用当前默认市场参数，不影响现有生成链路。
- 涉及文件：
  - `components/create-job-form.tsx`
- 待办事项 / 风险：
  - 建议在提示词模式下实际切换一次开关并刷新页面，确认草稿恢复时展开状态符合预期。
  - 当前只调整了提示词模式；标准、套图、亚马逊 A+、参考图复刻的市场参数位置不变。
# 2026-03-12

## 阶段：参考图复刻失败率与重复提示词修复

- 阶段目标：修复参考图复刻最新任务容易失败的问题，同时去掉失败详情里“严格提示词 + 回退提示词”被连续展示、看起来像发送了两次的现象。
- 已完成事项：
  - 排查数据库中的最新失败任务，确认最新参考图复刻任务实际使用的是 `zh-CN`，不是日文；失败原因是参考图复刻链路的重建提示词过于严格，模型两次都拒绝生成。
  - 在 `lib/generation.ts` 中移除参考图复刻的“严格一次 + 回退一次”双请求逻辑，改为只发送一次重建请求，避免同一任务里连续发送两条 remake prompt。
  - 在 `lib/templates.ts` 中收紧并简化 `buildReferenceDirectRemakePrompt`，保留核心的海报结构复刻约束，但删除过强、过重复的 strict/fallback 话术，降低模型失败率。
  - 让后续失败任务只记录一份实际发送给模型的参考图复刻 prompt，不再把两段 prompt 拼接进详情页。
  - 完成 `npm run typecheck` 校验。
- 涉及文件：
  - `lib/generation.ts`
  - `lib/templates.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 这次修复只作用于新任务，历史上已经失败并落库的任务不会自动改写错误信息或 prompt 展示内容。
  - 仍建议实际再跑一次参考图复刻任务，确认单次重建提示词在当前 Gemini / 中转配置下稳定可用。
# 2026-03-12

## 阶段：历史记录增加已完成任务生成耗时

- 阶段目标：在历史记录的“统计”区域补充任务生成耗时，只对状态为“已完成”的任务显示，避免处理中和失败任务出现误导性时长。
- 已完成事项：
  - 在 `components/job-table.tsx` 中新增任务耗时格式化逻辑，基于 `createdAt` 与 `completedAt` 计算时长。
  - 将耗时显示放入历史记录表格的“统计”格内，与总数、成功数、失败数放在同一区域。
  - 限制只有 `completed` 状态且存在 `completedAt` 的任务才显示耗时，其它状态不显示。
  - 在 `app/globals.css` 中补充统计区与耗时文案样式，保持现有表格宽度不再继续扩张。
- 涉及文件：
  - `components/job-table.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前“部分完成”任务暂不显示耗时；如果后续希望也展示，可再明确是按任务结束时间还是按当前更新时间计算。
# 2026-03-12

## 阶段：v0.3.1 对外发布整理

- 阶段目标：将当前版本整理为 `v0.3.1` 对外发布版，移除 GitHub 仓库中的开发记录文件，生成新的安装包并准备版本说明。
- 已完成事项：
  - 检查当前开发记录位置，确认仓库中的开发记录主要为 `doc/进展记录.md`。
  - 将版本号从 `0.3.0` 升级到 `0.3.1`。
  - 新增对外版本说明 `Readme/版本说明-v0.3.1.md`。
  - 将 `doc/进展记录.md` 加入 `.gitignore`，准备从 GitHub 仓库中移除，仅保留本地使用。
- 涉及文件：
  - `package.json`
  - `package-lock.json`
  - `.gitignore`
  - `Readme/版本说明-v0.3.1.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 仍需完成 Git 提交、tag、安装包生成、远端推送和 Release 发布。
  - 如果 GitHub Release 发布权限未登录，可能需要单独补最后一步网页发布。

## 阶段：v0.3.1 安装包生成与对外发布准备

- 阶段目标：完成 0.3.1 安全安装包生成，并整理 GitHub 推送与 Release 发布所需材料。
- 已完成事项：
  - 执行 
pm run build，确认当前版本可正常构建。
  - 执行 
pm run package:installer:exe:safe，生成新的安全安装器 elease/IMAGE-STUDIO-WINDOWS-0.3.1.exe。
  - 确认版本化安装器已按既有命名规则输出。
- 涉及文件：
  - elease/IMAGE-STUDIO-WINDOWS-0.3.1.exe
  - Readme/版本说明-v0.3.1.md
  - 本地 doc/进展记录.md
- 待办事项或风险：
  - 仍需完成 Git 提交、tag 推送以及 GitHub Release 正式发布。
  - 当前 GitHub CLI 未登录，Release 直发可能受限，需要根据现有 Git 凭据或网页权限决定最后一步。

## 2026-03-12 GitHub v0.3.1 发布收尾
- 阶段目标：从 GitHub 仓库移除开发记录，并安全发布 v0.3.1 安装器与版本说明。
- 已完成事项：将 doc/进展记录.md 从 Git 跟踪中移除并加入 .gitignore；同步代码到 GitHub；创建 0.3.1 标签；发布 GitHub Release；上传 IMAGE-STUDIO-WINDOWS-0.3.1.exe；新增 Readme/版本说明-v0.3.1.md。
- 涉及文件：C:\Users\AEboli\Documents\Codex\.gitignore、C:\Users\AEboli\Documents\Codex\package.json、C:\Users\AEboli\Documents\Codex\package-lock.json、C:\Users\AEboli\Documents\Codex\Readme\版本说明-v0.3.1.md。
- 待办事项或风险：远端 Release 已上线；若后续继续发布，需要延续“本地保留进展记录、仓库不提交”的策略。

## 2026-03-12 提示词开关回看与飞书结构重整
- 阶段目标：在任务详情页直观看到提示词模式的“翻译 / 优化”开关状态，并整理一份可直接重建的飞书字段结构。
- 已完成事项：在 `components/job-details-client.tsx` 的任务头部新增“提示词翻译”“真实照片优化”状态展示；扩展飞书字段映射，新增 `promptTranslation` 与 `promptOptimization` 两个可选字段；更新设置页支持字段说明与推荐映射模板；更新 `Readme/使用说明-电商AI出图工作台.md`，补充飞书字段类型、推荐映射 JSON 和提示词模式四种组合说明。
- 涉及文件：`components/job-details-client.tsx`、`lib/types.ts`、`lib/feishu-field-mapping.ts`、`lib/feishu.ts`、`components/settings-form.tsx`、`Readme/使用说明-电商AI出图工作台.md`、`doc/进展记录.md`。
- 待办事项或风险：历史任务默认会按兼容策略把未显式记录的“提示词翻译”视为开启；如果后续希望在历史记录列表或飞书表中进一步筛选这两个开关，可以再补对应筛选项。

## 2026-03-12 飞书默认口径统一为“生图模式 / 语言”
- 阶段目标：让飞书推荐字段结构与网页当前口径保持一致，避免继续沿用“平台 / 市场”式命名造成理解偏差。
- 已完成事项：将飞书推荐映射中的 `mode` 字段标题从“创作模式”调整为“生图模式”；从默认推荐结构中移除 `platform`、`country` 两列，仅保留“生图模式 + 语言”作为核心口径；同步更新设置页提示文案与 `Readme/使用说明-电商AI出图工作台.md` 中的飞书字段清单、推荐映射 JSON 和历史记录说明；保留 `platform`、`country` 技术字段兼容能力，旧表仍可继续使用。
- 涉及文件：`lib/feishu-field-mapping.ts`、`lib/feishu.ts`、`components/settings-form.tsx`、`Readme/使用说明-电商AI出图工作台.md`、`doc/进展记录.md`。
- 待办事项或风险：如果你现有飞书多维表还保留旧的“平台 / 国家”列，系统不会报错，但新版推荐模板不会再默认填充它们；若要完全统一展示口径，建议按新字段重建或手动停用旧列。

## 2026-03-12 飞书模式值统一为通用口径
- 阶段目标：把飞书 `mode` 字段实际写入的值统一成一套可跨全部模式复用的命名规则，并同步沉淀到推荐模板说明中。
- 已完成事项：将 `lib/feishu.ts` 中的模式值统一为“标准模式、提示词模式、参考图复刻模式、套图模式、亚马逊A+模式”；更新设置页飞书映射提示，明确如果“生图模式”使用单选字段，建议直接按这五个选项建值；更新 `Readme/使用说明-电商AI出图工作台.md`，补充生图模式字段的推荐类型与统一枚举值，确保重建飞书表时有固定参考。
- 涉及文件：`lib/feishu.ts`、`components/settings-form.tsx`、`Readme/使用说明-电商AI出图工作台.md`、`doc/进展记录.md`。
- 待办事项或风险：当前网页详情页和部分界面文案仍保留“标准出图 / 参考图复刻”等展示口径；如果后续希望前端页面也完全统一成“XX模式”体系，可以再做一轮全局文案对齐。

## 2026-03-12 参考图复刻改为单一路径直驱还原
- 阶段目标：将参考图复刻收敛成“只保留图1基础参数，直接把原图主体还原进参考图”的最小流程，移除额外分析与改写能力。
- 已完成事项：在 `components/create-job-form.tsx` 中移除参考图复刻的复刻强度、保留参考文字、补充提示词、负向提示词和 JSON 覆盖入口；将参考图上传限制为只取第一张；更新模式说明与摘要文案，明确只保留比例、分辨率和数量三项基础参数；在 `app/api/generate/route.ts` 与 `lib/job-builder.ts` 中把参考图复刻统一规范为单图参考、单类型 `scene`、单比例、单分辨率、固定 `referenceStrength = "reference"`、`preserveReferenceText = true`，并清空附加覆盖项；在 `lib/generation.ts` 中移除参考图分析与海报槽位生成链路，改为直接使用源图和第一张参考图生成；在 `lib/templates.ts` 中把参考图复刻 prompt 收敛为高保真参考图还原指令；在 `components/job-details-client.tsx` 中把参考图复刻结果的图片类型文案改为“复刻图”，并隐藏已不再可控的复刻强度展示。
- 涉及文件：`components/create-job-form.tsx`、`app/api/generate/route.ts`、`lib/job-builder.ts`、`lib/generation.ts`、`lib/templates.ts`、`components/job-details-client.tsx`、`doc/进展记录.md`。
- 待办事项或风险：当前数据库字段中仍保留 `referenceStrength`、`preserveReferenceText` 和参考图分析相关字段，主要为了兼容旧任务与重试；如果后续确定彻底不再需要，可再做一轮存储层与详情页的彻底清理。

## 2026-03-12 参考图复刻切换为“严格还原 + 文案来源”模式
- 阶段目标：把参考图复刻进一步收敛成“参考图是母版、原图只替换主体与同类关联元素”的严格还原链路，并增加“完全参照原图 / 按文案表格”两个明确的文案来源选项。
- 已完成事项：在 `lib/types.ts`、`lib/db.ts`、`lib/job-builder.ts` 和 `app/api/generate/route.ts` 中新增并打通 `referenceCopyMode` 字段，默认值为 `reference`，旧任务重试也会自动按该默认值处理；在 `lib/generation.ts` 中让参考图复刻不再走市场语言驱动的输入翻译，并统一只使用第一张参考图；在 `lib/templates.ts` 中重写参考图复刻 prompt，明确“参考图尽量不变，只替换主体和所有同类关联元素”，并针对 `reference` / `copy-sheet` 两种文案来源给出不同约束；在 `components/create-job-form.tsx` 中新增“文案来源”切换，默认“完全参照原图”，切到“按文案表格”时再展开图片名、品牌名、核心卖点、补充说明字段；在 `components/job-details-client.tsx` 中移除参考图分析 tab 与相关展示，改为在任务头部显示“文案来源”；同步更新 `Readme/使用说明-电商AI出图工作台.md` 和 `Readme/PRD-电商AI出图工作台.md` 的参考图复刻规则说明。
- 涉及文件：`lib/types.ts`、`lib/db.ts`、`lib/job-builder.ts`、`app/api/generate/route.ts`、`lib/generation.ts`、`lib/gemini.ts`、`lib/templates.ts`、`components/create-job-form.tsx`、`components/job-details-client.tsx`、`Readme/使用说明-电商AI出图工作台.md`、`Readme/PRD-电商AI出图工作台.md`、`doc/进展记录.md`。
- 待办事项或风险：数据库和详情页中仍保留参考图分析相关存储字段，仅用于兼容旧任务读取；如果后续确认不再需要历史分析信息，可以再做一轮数据库字段与同步逻辑的彻底清理。`npm run build` 仍可能继续出现既有的 `node:sqlite` experimental warning 和 Windows standalone `copyfile EINVAL` 提示，需要和本次改动分开看待。

## 2026-03-12 设置页改成并行卡片布局，品牌库下沉到底部
- 阶段目标：把设置页从“长表单 + 右侧品牌库”改成更清晰的并行设置工作台，让 Gemini、飞书同步、素材与任务配置更容易扫读和测试，并把品牌库移到页面底部。
- 已完成事项：在 `components/settings-form.tsx` 中重构设置页表单结构，将 Gemini / 中转设置、飞书多维表格同步、素材与任务三个板块拆成独立卡片；把 Gemini 和素材配置放在上方并行区，飞书同步扩成下方宽卡；新增每个板块的说明文案、素材目录与并发说明、统一保存说明；在 `app/settings/page.tsx` 中移除原来的左右分栏布局，改为先展示设置卡片区，再在页面底部单独展示品牌库；在 `app/globals.css` 中补充新的 settings 栅格、字段区、卡片 footer 和保存区样式，并补齐窄屏单列响应式。
- 涉及文件：`components/settings-form.tsx`、`app/settings/page.tsx`、`app/globals.css`、`doc/进展记录.md`。
- 待办事项或风险：目前仅完成布局与信息层级优化，品牌库本身的交互没有改动；如果后续希望继续提升设置页体验，可以再补“保存状态固定吸底”“测试成功态高亮”“飞书字段映射折叠区”等细化交互。`npm run typecheck` 已通过。
## 2026-03-12 参考图复刻失败修复 - 第一阶段
- 阶段目标：先修底层可靠性骨架，解决参考图复刻任务在进程重启后卡住、续跑会重复处理已完成项的问题，并为请求阶段失败补充独立诊断字段。
- 已完成事项：
  - 在 `lib/types.ts` 中扩展 `ProviderDebugInfo`，新增 `provider-request` 失败阶段以及 `attempt`、`maxAttempts`、`requestImageCount`、`requestBytes` 调试字段。
  - 在 `lib/db.ts` 中新增可恢复任务查询、将残留 `processing` 项重置为 `queued`、任务回到 `queued` 状态、按 item 状态汇总任务进度等 helper。
  - 在 `lib/queue.ts` 中加入首次启动恢复扫描逻辑，并新增 `ensureQueueReady()` 入口。
  - 新增 `lib/runtime.ts`，并在 `app/layout.tsx` 中接入运行时初始化，保证服务恢复后会触发队列自恢复。
  - 在 `lib/generation.ts` 中将任务执行改为只处理 `queued` 项，并在结束后基于数据库真实 item 状态回收任务状态，避免恢复后重复重跑成功项。
- 涉及文件：
  - `lib/types.ts`
  - `lib/db.ts`
  - `lib/queue.ts`
  - `lib/runtime.ts`
  - `app/layout.tsx`
  - `lib/generation.ts`
- 待办事项或风险：
  - 还需补齐 Gemini 请求层的瞬时压缩、网络重试和更清晰的失败归因展示。
  - 还未验证恢复后的卡住任务是否会在当前环境中自动续跑，需要在全部改完后结合类型检查和实际数据库状态一起验证。

## 2026-03-12 参考图复刻失败修复 - 第二阶段
- 阶段目标：补齐 Gemini 请求层的稳定性，包括大图上传瞬时压缩、可重试网络错误重试、失败阶段分类，并把当前环境中的并发止血到 `2`。
- 已完成事项：
  - 在 `lib/gemini.ts` 中接入 `sharp` 预处理，provider 上传前会按最长边 `3072px`、目标体积 `8MB` 进行瞬时压缩，不会改写本地原始素材文件。
  - 为 Gemini 请求增加 3 次网络层重试，仅对 `fetch failed`、超时、连接重置、`429/5xx` 等可重试错误生效，并补充退避等待。
  - 将 `normalizeProviderError` 对请求阶段失败的输出改为明确的“provider 请求未返回响应”，不再只落成裸 `fetch failed`。
  - 在 `components/job-details-client.tsx` 中补充失败阶段展示，并在任务摘要中区分请求阶段失败和 provider 图片下载失败。
  - 在 `lib/feishu.ts` 中扩展生成统计摘要，失败项会按“请求失败 / 下载失败 / 模型拒绝或响应异常”分类汇总。
  - 通过 `npm install sharp@0.34.5 --save-exact` 将 `sharp` 提升为直接依赖。
  - 已将当前本地数据库中的 `settings.max_concurrency` 从 `5` 调整为 `2`，作为立即止血措施。
- 涉及文件：
  - `lib/gemini.ts`
  - `components/job-details-client.tsx`
  - `lib/feishu.ts`
  - `package.json`
  - `package-lock.json`
  - `data/commerce-image-studio.sqlite`
- 待办事项或风险：
  - 还需要跑 `npm run typecheck`、`npm run build`，并确认恢复逻辑已经把历史卡住任务重新拉起。
  - `sharp` 当前新增为直接依赖，需要在本机验证 Next 构建与 Windows 打包链路没有新增兼容问题。

## 2026-03-12 参考图复刻失败修复 - 验证与收尾
- 阶段目标：完成类型检查、生产构建验证，并确认历史卡住任务已被恢复逻辑重置到可续跑状态。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过；构建期间仍有仓库既有的 `node:sqlite` experimental warning 和 Windows traced files `copyfile EINVAL` 提示，但没有新增失败。
  - 核对数据库中的历史任务状态：`job_31166d98-170d-4eac-b83a-ea51454f14d8` 与 `job_93f4c8e4-0fe8-4e39-8328-a14a3f12c455` 已从原先的 `processing` 恢复为 `queued`，其 `processing` item 也已重置为 `queued`，符合预期的恢复策略。
  - 当前数据库中的 `settings.max_concurrency` 已确认是 `2`。
- 涉及文件：
  - `lib/gemini.ts`
  - `lib/queue.ts`
  - `lib/generation.ts`
  - `app/layout.tsx`
  - `data/commerce-image-studio.sqlite`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 如果要让已经恢复为 `queued` 的历史任务立即续跑，还需要确保当前应用进程保持运行并触发一次正常的服务请求；本次修复已保证恢复能力，但不会在离线状态下自行生成结果。
  - 更早历史中的“模型明确拒绝”类参考图复刻失败仍然存在，本次没有改 prompt 策略去覆盖那一类问题。
## 2026-03-12 历史结果工作台改成单图聚焦
- 阶段目标：把历史记录详情页的结果工作台改成以单张生成图为核心的审核工作区，并把审核动作集中到“加入对比”左侧。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中把结果工作台改成“操作栏 + 单图舞台 + 右侧信息栏”结构，主区域只展示纯图片，不再并排展示文案图。
  - 将 `入选 / 通过 / 淘汰 / 重置 / 加入对比` 统一放进工作台头部右侧工具栏，桌面端同排，移动端由样式自动换行。
  - 给详情页主舞台图、对比图区图片、变体抽屉图片统一接入 `medium-zoom`，点击图片即可放大查看。
  - 变体浏览、详情抽屉、对比区全部改成只消费 `generatedAsset`，不再使用 `layoutAsset` 兜底预览。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 旧任务数据库里仍可能保留 `layoutAsset`，但当前前台已经不再显示；如后续确认彻底不需要，可再做数据清理。

## 2026-03-12 文案图链路停用与对外口径清理
- 阶段目标：把“文案图 / layout creative”从当前活跃产品流程和对外说明中移除，同时保留数据库兼容字段。
- 已完成事项：
  - 继续沿用前面已经完成的生成链路调整，新任务不再写入 `layout` 资产，审核通过 ZIP 也只打包纯图片。
  - 将任务详情成功数、已通过下载数等口径统一为只认 `generatedAsset`。
  - 清理 README、PRD、使用说明中的“文案图 / layout creative / 排版图”对外描述，和当前页面行为保持一致。
- 涉及文件：
  - `app/api/jobs/[id]/approved-download/route.ts`
  - `lib/db.ts`
  - `README.md`
  - `Readme/PRD-电商AI出图工作台.md`
  - `Readme/使用说明-电商AI出图工作台.md`
  - `使用说明-简体中文.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - `include_copy_layout`、`layout_asset_id` 以及历史 `layout` 资产仍保留在数据库层，仅作兼容，不再进入当前前台流程。

## 2026-03-12 历史结果工作台重构验证
- 阶段目标：确认结果工作台重构和文案图停用没有引入类型错误或生产构建问题。
- 已完成事项：
  - 跑通 `npm run typecheck`。
  - 跑通 `npm run build`。
  - 确认详情页中不再残留“文案图 / layout creative”文案，结果区与对比区均可复用同一套图片放大交互。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `README.md`
  - `Readme/PRD-电商AI出图工作台.md`
  - `Readme/使用说明-电商AI出图工作台.md`
  - `使用说明-简体中文.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 构建阶段如果仍出现仓库原有的 `node:sqlite` experimental warning 或 Windows `copyfile EINVAL` 提示，需要与本次 UI 调整分开看待。
## 2026-03-12 视频微交互分析与创作台提交反馈优化
- 阶段目标：参考外部视频里的“禁用按钮被点击时轻微抖动”交互，优化创作台提交按钮在受阻状态下的反馈体验。
- 已完成事项：
  - 下载并分析外部视频 `shake-disabled-button-while-clicking.mp4`，确认其核心是“按钮当前不可执行时，点击仍然有明确的阻止反馈，而不是静默无响应”。
  - 在 `components/create-job-form.tsx` 中为创作台提交区新增受阻提交判断，覆盖缺少原图、缺少参考图、提示词为空、标准模式未填图片名、套图模式缺必填字段等场景。
  - 将提交按钮改成“视觉禁用但仍可点击反馈”的主按钮：受阻时点击会立即显示对应错误、触发轻微抖动，并自动把焦点带回最需要补的输入区域。
  - 为原图上传、参考图上传、提示词输入、套图必填字段补充 refs，确保反馈后可以直接定位到缺失项。
  - 在 `app/globals.css` 中新增 `is-blocked / is-shaking` 按钮样式与横向轻抖动画，保留现有深色视觉体系。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前这套“受阻反馈”先落在最核心的创作台提交动作；如后续验证体验有效，可再扩展到设置页保存、飞书测试、任务详情里的其他受阻按钮。

## 2026-03-12 视频交互优化验证
- 阶段目标：确认创作台受阻提交反馈没有引入类型错误或构建问题。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 确认此次改动没有引入新的构建报错；构建阶段仍保留仓库原本就存在的 `node:sqlite` experimental warning。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 还需要在你实际使用的创建流程里体感确认抖动幅度是否合适；如果觉得偏强或偏弱，可以继续微调动画位移和时长。

## 2026-03-12 全局 Design Tokens 与 8pt Grid UI 重构
- 阶段目标：把现有工作台从“功能可用”重构为更统一、更高级的产品级 Web 界面，严格落地 8pt grid 和 design tokens 排版层级。
- 已完成事项：
  - 新增 `app/theme-refactor.css`，建立统一设计 token 体系，覆盖 8pt 间距刻度、圆角、阴影、颜色、容器宽度、标题层级与正文层级。
  - 在 `app/layout.tsx` 中接入 `Plus Jakarta Sans` 与 `Space Grotesk` 字体，并把全局主题样式挂入根布局，统一页面基准视觉。
  - 重构导航头部，补充品牌副标题、状态徽标和更清晰的产品化信息结构，提升首页与全站入口辨识度。
  - 重构首页、创作页、历史页、设置页、模板中心页顶部 Hero 区，统一为“主叙事 + KPI 面板”的产品工作台表达方式。
  - 为统计卡片、表单、面板、筛选区、表格、设置区、模板区和详情相关卡片补齐 token 化样式，使信息密度、间距节奏和层级关系保持一致。
  - 清理品牌库标题中的版本化文案，把界面口径收敛到更适合长期交付的产品表达。
- 涉及文件：
  - `app/theme-refactor.css`
  - `app/layout.tsx`
  - `app/page.tsx`
  - `app/create/page.tsx`
  - `app/history/page.tsx`
  - `app/settings/page.tsx`
  - `components/navigation.tsx`
  - `components/stat-card.tsx`
  - `components/template-center-client.tsx`
  - `components/brand-library-manager.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前主要完成了全站骨架、核心入口页和关键工作台模块的视觉重构，`create-job-form`、历史详情工作台等深层交互区域后续仍可继续做更细的组件级统一。
  - 主题样式目前是增量覆盖在既有 `app/globals.css` 之上，后续如果要继续长期演进，建议再做一轮样式职责收敛，减少双层样式来源。

## 2026-03-12 UI 重构验证
- 阶段目标：确认本轮高级感 UI 重构没有引入类型错误、构建问题或关键页面渲染异常。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 使用浏览器实际核对 `/`、`/create`、`/history`、`/settings`、`/templates` 页面，确认新的 Hero、KPI、导航和面板结构已渲染生效。
- 涉及文件：
  - `app/theme-refactor.css`
  - `app/page.tsx`
  - `app/create/page.tsx`
  - `app/history/page.tsx`
  - `app/settings/page.tsx`
  - `components/template-center-client.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 构建阶段仍保留仓库既有的 `node:sqlite` experimental warning，需要与本次 UI 重构分开看待。
  - 当前视觉验收覆盖了核心入口页，后续如果你要继续推进交付版，我建议再补一轮针对任务详情页、创作表单深区和移动端断点的精修。

## 2026-03-12 UI 尺寸体系二次收紧
- 阶段目标：把本轮新界面进一步收紧到固定的产品级尺寸规范，只允许使用 `4 / 8 / 12 / 16 / 24 / 32 / 40 / 48` 这组 spacing，并统一 Card、Button、Input、Icon 的尺寸口径。
- 已完成事项：
  - 在 `app/theme-refactor.css` 中重写 spacing token，只保留 `4 / 8 / 12 / 16 / 24 / 32 / 40 / 48` 八档尺寸。
  - 新增 `--card-padding`、`--card-gap`、`--control-height`、`--icon-size-sm`、`--icon-size-md` 这些语义 token，用于统一卡片内边距、卡片间距、控件高度和图标尺寸。
  - 把主导航按钮、语言切换、主次按钮、历史入口等标准按钮统一到 `40px` 高；把输入框统一到 `40px` 高。
  - 把 `page-kpi`、`panel`、`create-panel`、`settings-card`、`template-card`、详情区子卡片等核心卡片统一到 `24px` 内边距，并把主要卡片容器的 gap 统一到 `16px`。
  - 补了一层高优先级覆盖，修正 `create-workspace.is-compact-viewport` 下旧样式把 `create-panel` 偷偷压回 `14px 16px` 的问题。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 这轮已经把主题层和创建页紧凑视口的核心尺寸规范收紧完成，但 `app/globals.css` 里仍保留了一些历史样式定义；当前已由主题层覆盖，后续最好做一轮彻底清理，避免长期双源维护。

## 2026-03-12 UI 尺寸规范验证
- 阶段目标：确认新的 spacing / card / button / input 规范在真实页面中按预期生效。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行两轮 `npm run build`，通过。
  - 在浏览器中对 `/create` 做实时样式抽查，确认 `page-kpi` 内边距为 `24px`、`create-panel` 内边距为 `24px`、按钮高度为 `40px`、输入框高度为 `40px`、KPI 区与操作区 gap 为 `16px`。
  - 额外确认紧凑视口下的 `.create-workspace.is-compact-viewport .create-panel` 已恢复为 `24px` 内边距，不再回退到旧值。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前浏览器抽查聚焦在创建页和主题层的代表性样式，下一轮如果继续深挖，我建议把任务详情页、品牌库卡片和模板编辑区也做同样的尺寸抽样验收。

## 2026-03-12 创作模式卡原生输入框泄漏修复
- 阶段目标：修复创建页点击“提示词模式”等创作模式卡片时，卡片内部错误露出原生 `radio` 输入框的问题。
- 已完成事项：
  - 定位到问题根因在 `app/theme-refactor.css`：此前把所有 `input` 统一套用了文本输入框样式，导致 `radio` 也继承了输入框的边框、背景和高度。
  - 将文本输入框样式收敛为仅命中文本类输入，不再影响 `radio / checkbox / file / hidden`。
  - 为 `.chip input` 增加统一的无障碍隐藏规则，保留语义和可点击性，但不再显示浏览器原生控件外观。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 这轮已修复模式卡和所有 `.chip` 场景下的原生输入泄漏；后续如果继续做表单视觉统一，可以再系统性检查一次 `checkbox-row`、品牌库选择器和模板筛选控件的原生控件呈现方式。

## 2026-03-12 模式卡输入框泄漏验证
- 阶段目标：确认创建页模式卡中的原生输入框已经被正确隐藏，且不影响现有交互。
- 已完成事项：
  - 执行 `npm run build`，通过。
  - 在浏览器中重新打开 `/create`，确认模式卡对应的 `input[name="creation-mode"]` 已变为 `position: absolute`、`width/height: 1px`、`opacity: 0`、`pointer-events: none` 的隐藏态。
  - 同时确认 `.chip` 仍保持 `40px` 高度和卡片式点击结构，创建页其余输入框与按钮没有被这次修复带坏。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前已经确认模式卡本身不再露出系统输入框；如果你后面继续发现别的表单区还有类似“原生控件漏出来”的情况，可以沿着同一思路继续排查是否被全局输入样式误命中。

## 2026-03-12 RGB 三元配色体系重构
- 阶段目标：把现有工作台的颜色体系重构为以蓝、绿、红为核心的产品视觉语言，并允许使用三色相融后的过渡色。
- 已完成事项：
  - 在 `app/theme-refactor.css` 中重写主色 token，新增 `--color-blue`、`--color-green`、`--color-red`，并补充 `--color-cyan`、`--color-violet`、`--color-solar` 等由三原色混合得到的辅助色。
  - 将 `accent / signal / success / warning / danger` 这一整组语义色全部收口到 RGB 体系，不再沿用旧的金色主提示口径。
  - 重构全局背景、头部、面板、KPI 卡片、主按钮、Ghost 按钮、选中态 Chip、预览舞台和提交条的渐变与光晕，让蓝绿红的层次从背景到交互面一体化生效。
  - 同步调整状态类 UI，包括成功、失败、处理中、完成、详情页标签激活态和设置引导卡片，使状态色和主题色不再割裂。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 RGB 配色已覆盖主题层和核心交互面，但还没有为“不同业务模块使用哪一种主色更占优”做更细的品牌语义分工；后续如果继续深化，可以再细分为“蓝主导航、绿成功效率、红风险提醒”的更强产品规则。

## 2026-03-12 RGB 配色验证
- 阶段目标：确认新的蓝绿红主题没有引入构建问题，并已在真实页面渲染生效。
- 已完成事项：
  - 执行 `npm run build`，通过。
  - 使用最新本地进程打开 `/create` 页面，确认创建页在新配色主题下可以正常渲染，且此前修过的模式卡原生输入框问题没有回归。
  - 再次确认旧金色与旧蓝紫系硬编码已从 `app/theme-refactor.css` 中清理，主题色已统一收口到新的 RGB token。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前验收以创建页为主，下一轮如果继续做颜色精修，我建议把历史页状态胶囊、设置页引导卡片和详情页审核状态再做一轮“模块色权重”审校，避免局部过亮或过花。

## 2026-03-12 模块级配色权重细化
- 阶段目标：在 RGB 三元主题的基础上，继续拉开不同模块的颜色气质，让总览、创作、历史、设置、模板中心能被一眼区分。
- 已完成事项：
  - 为历史页、设置页、模板中心页补充独立页面根类，分别接入 `history-page`、`settings-page`、`template-center-page`，便于模块级主题覆盖。
  - 调整首页统计卡 accent 色，改为蓝、红、青、绿四组更贴近当前 RGB 主体系的标记色。
  - 在 `app/theme-refactor.css` 中新增模块级背景和按钮配色规则：
    - 总览页偏蓝青，强调全局视图与入口感。
    - 创作页偏蓝青绿，突出生产流与生成动作。
    - 历史页偏蓝红，强化数据回看与异常追踪感。
    - 设置页偏绿青，强化配置、连接、稳定性的控制台气质。
    - 模板中心偏蓝紫青，强化规则系统和策略编辑感。
  - 将模块色权重覆盖到 Hero、主 Panel、部分卡片、主按钮和关键选中态，不再只有全站统一色而缺少页面识别度。
- 涉及文件：
  - `app/page.tsx`
  - `app/history/page.tsx`
  - `app/settings/page.tsx`
  - `components/template-center-client.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前模块色已经拉开，但仍属于“同品牌下的页面区分”，还没有继续细化到每个业务子块的颜色语义；后续如果继续深化，可以再给“审核 / 失败 / 配置测试 / 模板编辑”做更细的子模块级配色规则。

## 2026-03-12 模块级配色验证
- 阶段目标：确认模块级配色权重已在多个页面真实生效，而不是只停留在 token 层。
- 已完成事项：
  - 执行 `npm run build`，通过。
  - 使用浏览器顺序核对 `/`、`/create`、`/history`、`/settings`、`/templates` 五个页面，并读取其 Hero、主按钮和主 Panel 的实际 `backgroundImage`。
  - 确认五个页面已经呈现不同的配色梯度：
    - 总览：蓝青红轻混色。
    - 创作：蓝青绿偏生产型。
    - 历史：蓝红偏分析回看型。
    - 设置：绿青偏控制台型。
    - 模板：蓝紫青偏规则系统型。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器在设置页输出了既有的“Multiple forms should be contained...” DOM 提示，这轮没有处理它；如果后续继续清理结构层，可以再单独检查设置页的 form 嵌套关系。

## 2026-03-12 页面级交互色阶统一
- 阶段目标：为总览、创作、历史、设置、模板中心分别建立更高级的 `hover / active / focus` 色阶，让交互反馈既统一规则，又保留页面自己的颜色气质。
- 已完成事项：
  - 在 `app/theme-refactor.css` 中新增页面级交互 token，补齐 `interactive-border-hover / active`、`interactive-surface-hover / active`、`interactive-shadow-hover / active`、`interactive-focus-ring`。
  - 将导航、语言切换、主按钮、Ghost 按钮、Chip、Tab、状态胶囊、预览缩略图、品牌卡片、概览卡片、模板卡片等交互态统一收口到同一套语义样式。
  - 为输入框、下拉框、文本域补充页面级 `hover / focus` 反馈，不再只使用全局固定青色聚焦环。
  - 将模块页根容器下放为真实的交互 token 计算节点，修正之前“页面 accent 已分开，但派生交互 token 仍按全局颜色计算”的问题。
  - 统一已选中态的边框与 focus ring 口径，让 `chip.is-active`、`detail-tab-button.is-active`、`variant-browser-card.is-active`、`brand-card-button.is-active`、`preview-thumb.is-active` 的反馈层级更一致。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前已经统一到页面级交互层，但还没有继续细分到“审核风险 / 错误告警 / 成功确认 / 次要说明”这类更细的业务语义层；如果下一轮继续精修，可以把红色进一步收敛到异常和审核链路上。

## 2026-03-12 页面级交互色阶验证
- 阶段目标：确认新的交互色阶不只存在于 CSS 定义里，而是已经在真实页面中以不同页面主题生效。
- 已完成事项：
  - 执行 `npm run build`，通过。
  - 启动本地服务后，用浏览器实际读取 `/create`、`/settings`、`/templates` 的按钮与输入框 `backgroundImage / boxShadow`，确认：
    - 创作页保持蓝青绿反馈。
    - 设置页主按钮保持绿青蓝反馈。
    - 模板页交互焦点切换为蓝紫青反馈。
  - 额外核对 `/` 和 `/history` 的卡片与入口按钮悬停态，确认：
    - 总览页卡片 hover 保持蓝青偏向。
    - 历史页入口按钮 hover 已带上蓝红混色和偏红光晕。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器核对过程中仍出现既有的 `favicon.ico 404` 与设置页 `Multiple forms should be contained...` 提示，这轮未处理；它们不影响本次交互色阶落地，但后续做交付级收尾时建议一起清掉。

## 2026-03-12 图片内存占用优化
- 阶段目标：降低历史页、结果详情页和创作预览区因为大图直接解码导致的浏览器与本地服务内存占用。
- 已完成事项：
  - 为 `app/api/assets/[assetId]/route.ts` 增加按展示尺寸输出图片的能力，支持通过 `w / q` 参数返回压缩后的 WebP 预览图，不再让所有缩略图直接请求原图。
  - 将原图返回链路改为流式输出，避免服务端每次读取资产都整块把原图 Buffer 常驻到内存里。
  - 为 Sharp 增加较保守的缓存上限，限制连续预览请求下的进程内缓存膨胀。
  - 新增 `lib/asset-url.ts`，统一构建原图、下载图、缩略图、预览图 URL。
  - 历史页缩略图改为 `160px` 级别的小图，详情页侧边素材改为 `256px`，变体浏览卡改为 `320px`，对比图改为 `720px`，主预览图改为 `1400px` 级别预览，同时把 zoom 源单独指回原图。
  - 为历史页、详情页和创作页的图片补充 `loading=\"lazy\"` 与 `decoding=\"async\"`，减少首屏一次性解码压力。
- 涉及文件：
  - `app/api/assets/[assetId]/route.ts`
  - `lib/asset-url.ts`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `components/create-job-form.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前优化主要集中在图片传输与解码链路，尚未对历史页做分页或虚拟列表；如果后续任务量进一步增长，仍建议继续控制一次性渲染的记录数。

## 2026-03-12 图片内存优化验证
- 阶段目标：确认这轮不是“看起来改了”，而是页面实际已经在请求更小的图。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 浏览器实测历史页首张缩略图请求已经变为 `?w=160&q=70`，返回 `image/webp`。
  - 对同一资源做对比抓取，确认：
    - 缩略图响应大小约 `6,908 bytes`。
    - 原图响应大小约 `40,962,936 bytes`。
  - 浏览器实测详情页图片源已分层：
    - 变体卡片缩略图：`?w=320&q=72`
    - 主预览图：`?w=1400&q=82`
    - zoom 源仍指向原图，保留放大查看能力。
- 涉及文件：
  - `app/api/assets/[assetId]/route.ts`
  - `lib/asset-url.ts`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `components/create-job-form.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 构建阶段仍会出现仓库原有的 `node:sqlite` experimental warning；这轮没有处理它。

## 2026-03-12 历史页分页降载
- 阶段目标：继续降低历史页的一次性渲染量，避免在有较多任务记录时把整张表和全部缩略图同时挂到页面里。
- 已完成事项：
  - 为 `lib/db.ts` 增加分页版 `listJobs(filters, { limit, offset })`，并新增 `summarizeJobs(filters)`，把“当前页数据”和“全量统计”分开查询。
  - 将 `app/history/page.tsx` 改造成分页页面，默认每页只渲染 `24` 条历史任务。
  - 为历史页增加分页导航、当前显示区间摘要，并保持筛选参数翻页透传。
  - 将分页按钮的 `active / disabled / mobile` 样式补入 `app/theme-refactor.css`，避免回退到旧样式口径。
- 涉及文件：
  - `lib/db.ts`
  - `app/history/page.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前分页优先解决“同时渲染过多记录”的问题，但历史页仍然是整页刷新式翻页；如果后续继续做更丝滑的体验，可以再考虑局部更新或虚拟列表。

## 2026-03-12 历史页分页验证
- 阶段目标：确认历史页分页不是只做了 UI，而是真的把当前页渲染量压下来了。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 浏览器实测 `http://127.0.0.1:3041/history`：
    - 第 1 页实际渲染 `24` 行。
    - 顶部分页摘要显示 `当前显示 1-24 / 66`。
    - 分页条显示 `1 / 2 / 3`。
  - 浏览器继续访问第 2 页：
    - URL 正常切换到 `?page=2`。
    - 分页摘要显示 `当前显示 25-48 / 66`。
    - 第 2 页实际渲染仍为 `24` 行。
  - 同时确认顶部 KPI 仍显示全量筛选集的任务总数，而不是只统计当前页。
- 涉及文件：
  - `lib/db.ts`
  - `app/history/page.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍有既有的 `favicon.ico 404`，这轮未处理。

## 2026-03-12 历史页表格布局修正
- 阶段目标：修复历史页在较窄工作区下预览缩略图挤压模式列的布局问题，并把上一页 / 下一页补到任务明细头部摘要区。
- 已完成事项：
  - 在 `components/job-table.tsx` 中为历史表格补充 `colgroup` 和列级 class，明确图片区、模式列、语言列、统计列、状态列与操作列的结构边界。
  - 在 `app/theme-refactor.css` 中把历史表格改成固定列宽策略，并为预览列、模式列、语言列、统计列、时间列补充 `min-width / ellipsis / nowrap` 规则，避免缩略图把文字列顶乱。
  - 将 `table-wrap` 调整为横向滚动容器，在视口不足时优先出现水平滚动，而不是让单元格互相挤压错位。
  - 在 `app/history/page.tsx` 的任务明细头部新增顶部分页摘要区，放置 `上一页`、`当前显示 x-y / total`、`下一页`，让翻页入口在表格顶部也能直接使用。
- 涉及文件：
  - `app/history/page.tsx`
  - `components/job-table.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前策略是在窄视口下用横向滚动保护表格结构；如果后续希望进一步提升小屏体验，可以继续考虑把部分列折叠成二级信息卡。

## 2026-03-12 历史页表格布局修正验证
- 阶段目标：确认历史页表格修正和顶部分页按钮已经在真实页面生效，而不是只停留在样式定义里。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并在浏览器中访问 `http://127.0.0.1:3042/history?page=2`，确认任务明细头部已经出现 `上一页`、`当前显示 25-48 / 66`、`下一页`。
  - 在接近用户截图的窄工作区宽度下复验历史表格，确认多张缩略图批次不再把模式列和语言列挤乱，表格在空间不足时改为横向滚动。
  - 保存验收截图到 `output/playwright/history-page-2-after-fix.png` 与 `output/playwright/history-page-2-narrow.png`，用于后续回看。
- 涉及文件：
  - `app/history/page.tsx`
  - `components/job-table.tsx`
  - `app/theme-refactor.css`
  - `output/playwright/history-page-2-after-fix.png`
  - `output/playwright/history-page-2-narrow.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次历史页表格修正无直接关系，后续可以单独清理。

## 2026-03-12 图片放大预览适配视口
- 阶段目标：让历史页和详情页的图片点击放大后自动按当前视口完整展示，不再沿用缩略图裁切逻辑放大。
- 已完成事项：
  - 新增 `components/image-lightbox.tsx`，提供统一的全屏预览层，支持背景点击关闭、Esc 关闭和页面滚动锁定。
  - 将 `components/job-table.tsx` 的历史缩略图点击行为从 `medium-zoom` 切换到新预览层，直接加载原图并按视口 `contain` 展示。
  - 将 `components/job-details-client.tsx` 的结果工作台、对比图和归档图点击预览入口一并切换到同一套预览层，避免详情页和历史页行为不一致。
  - 在 `app/globals.css` 中补充 `image-lightbox` 相关样式，明确 `max-width / max-height / object-fit: contain`，保证任何比例图片都优先完整适配窗口。
- 涉及文件：
  - `components/image-lightbox.tsx`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前预览层以“完整适配视口”为优先策略；对于超宽全景图，显示高度会相应变矮，这是符合比例约束的预期结果，如需后续支持局部拖拽放大可以再单独扩展。

## 2026-03-12 图片放大预览验证
- 阶段目标：确认新的图片预览层已经替代旧放大方式，并且真实按视口上限约束显示。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并在浏览器中访问 `http://127.0.0.1:3043/history?page=2`，点击历史缩略图后确认页面出现新的 `dialog` 预览层。
  - 启动本地服务并在浏览器中访问 `http://127.0.0.1:3043/jobs/job_86306538-3d65-4c45-a224-1fcc31362d8d`，点击结果工作台主图后确认详情页也已切换为同一套预览层。
  - 浏览器读取放大图实际样式，确认：
    - `object-fit: contain`
    - `max-width` 受视口宽度约束
    - `max-height` 受视口高度约束
  - 保存验收截图到 `output/playwright/image-lightbox-fit.png`，用于后续回看。
- 涉及文件：
  - `components/image-lightbox.tsx`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `app/globals.css`
  - `output/playwright/image-lightbox-fit.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次图片预览改造无直接关系。

## 2026-03-12 图片放大关闭交互修正
- 阶段目标：让放大预览在点击背景空白处或按 `Esc` 时都能立即关闭，交互行为符合常见查看器预期。
- 已完成事项：
  - 调整 `app/globals.css` 中 `image-lightbox-surface` 的布局范围，不再让其占满整屏，避免空白区域点击被内部容器拦截。
  - 将关闭按钮继续保留在预览卡片右上角附近，同时让真正的背景遮罩层负责接收“点击空白关闭”的交互。
  - 保留 `components/image-lightbox.tsx` 中既有的 `Escape` 监听逻辑，使键盘关闭和鼠标关闭走同一套退出流程。
- 涉及文件：
  - `components/image-lightbox.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前关闭按钮位置是相对图片卡片浮动；如果后续需要统一到视口固定角落，可以再单独做一轮视觉微调。

## 2026-03-12 图片放大关闭交互验证
- 阶段目标：确认放大后的图片可以通过背景点击和 `Esc` 两种方式关闭，而不是只依赖右上角关闭按钮。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并在浏览器中访问 `http://127.0.0.1:3044/jobs/job_86306538-3d65-4c45-a224-1fcc31362d8d`，打开图片预览后使用坐标点击左上角空白区，确认 `lightbox` 与 `dialog` 节点都已移除。
  - 再次打开同一张图片后按下 `Escape`，确认 `lightbox` 与 `dialog` 节点都已移除。
- 涉及文件：
  - `components/image-lightbox.tsx`
  - `app/globals.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次关闭交互修正无直接关系。

## 2026-03-12 生成项浏览与结果工作台交互重构
- 阶段目标：改善任务详情页中“生成项浏览”和“结果工作台”的切换路径，让当前焦点更明确、查看和切换更顺手。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中补充当前焦点索引、上一项 / 下一项切换逻辑，并让选中项变化后自动滚动到列表可视区域。
  - 将“生成项浏览”头部改为焦点摘要区，集中显示当前查看序号、当前项元信息和快速切换按钮，不再让用户只能从一堆卡片里自己找当前项。
  - 重构生成项卡片信息层级：缩略图、序号、图型标题、比例 / 分辨率、生成状态、审核状态、对比状态分层展示，减少原先重复而拥挤的文案堆叠。
  - 在“结果工作台”顶部新增当前项导航区，把当前查看信息和上一项 / 下一项切换放到工作台入口处，和右侧审核操作分开，减少操作挤在同一行的混乱感。
  - 在 `app/theme-refactor.css` 中为浏览器摘要区、卡片焦点态、状态胶囊、工作台导航区补充新的 token 化样式，统一交互反馈和视觉层级。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前仍保留生成项为卡片网格浏览；如果后续批次数继续增大，可以进一步考虑加“状态筛选 / 只看失败 / 只看已入选”之类的局部过滤。

## 2026-03-12 生成项浏览与结果工作台交互验证
- 阶段目标：确认这轮交互重构已经在真实页面生效，且上一项 / 下一项切换会同步驱动卡片焦点和工作台内容。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并在浏览器中访问 `http://127.0.0.1:3046/jobs/job_cffe61a2-1e6c-43ed-b6a7-ec1c2ef85350`，确认生成项浏览顶部已出现“当前查看 1 / 6”和上一项 / 下一项按钮。
  - 在浏览器中点击“下一项”后，确认：
    - 顶部摘要更新为“当前查看 2 / 6”
    - 当前激活卡片切换到新的生成项
    - 结果工作台的标题、图片、元信息和 Prompt 同步切换到新项
  - 保存验收截图到 `output/playwright/job-details-browser-workbench-redesign.png`，用于后续回看。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/theme-refactor.css`
  - `output/playwright/job-details-browser-workbench-redesign.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次交互重构无直接关系。
## 2026-03-12 生成项浏览并入结果工作台
- 阶段目标：把任务详情页里分离的“生成项浏览”和“结果工作台”合并成一个连续工作区，并把切换/审核按钮上移，让主图更早进入主视觉区域。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中保留旧浏览区结构但改为隐藏，避免大范围删除影响详情页其它逻辑。
  - 新增工作台头部焦点区，集中展示当前查看状态、当前项摘要、上一项/下一项，以及入选、通过、淘汰、重置、加入对比等按钮。
  - 把主图预览区直接放到新的工作台头部下方，让图片进入视线中心，不再被独立浏览面板和旧工具条压到下方。
  - 将生成项列表改为工作台内部的横向切换带，保留当前项高亮、状态胶囊和对比状态，继续支持点选切换。
  - 将旧浏览区卡片上的 `data-variant-card-id` 改为历史标识，避免自动定位逻辑误命中隐藏节点。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前生成项切换带放在工作台主图区之后，优先保证主图上移；如果后续还想进一步强调切换效率，可以再评估是否需要加入固定吸顶或局部悬停预览。

## 2026-03-12 生成项浏览并入结果工作台验证
- 阶段目标：确认新的合并布局已经真实生效，并且切换动作会同步驱动工作台主图和元信息更新。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并在浏览器中访问 `http://127.0.0.1:3047/jobs/job_cffe61a2-1e6c-43ed-b6a7-ec1c2ef85350`，确认页面不再出现独立的“生成项浏览”主面板。
  - 使用浏览器快照确认工作台顶部已经出现新的焦点摘要区和上移后的审核动作区，主图紧接在其下方显示。
  - 在浏览器中点击“下一项”，确认“当前查看 1 / 6”更新为“当前查看 2 / 6”，同时主图、下载链接、标题、卖点文案和底部切换带高亮项同步切换。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次详情页结构调整无直接关系。
## 2026-03-12 顶部头部取消吸顶
- 阶段目标：让全局顶部头部不要再跟随页面滚动吸附在视口顶部，而是作为普通内容随页面一起滚走。
- 已完成事项：
  - 在 `app/theme-refactor.css` 中将 `.app-header` 的桌面端布局从 `position: sticky` 改为 `position: static`。
  - 保留原有头部视觉样式、导航结构和语言切换布局，仅移除吸顶行为，不影响其它页面头部的排版节奏。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前是全站统一取消吸顶；如果后续只想让某些页面保留吸顶，需要再拆成页面级头部策略。

## 2026-03-12 顶部头部取消吸顶验证
- 阶段目标：确认顶部头部已经不再吸顶，而是会在页面滚动时正常离开视口。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并访问 `http://127.0.0.1:3048/history`。
  - 浏览器读取 `.app-header` 计算样式，确认 `position` 已为 `static`。
  - 浏览器将页面滚动到约 `900px` 后再次读取 `.app-header` 的视口坐标，`top` 已为负值，说明头部已随页面滚出视口，而不是继续停留在顶部。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次头部行为调整无直接关系。
## 2026-03-12 结果切换取消页面跟滚并补主图箭头
- 阶段目标：让任务详情页在切换上一项/下一项或点选生成项时不再自动拖动整页滚动，同时在主图两侧增加透明切换箭头，直接在图片区切换当前结果。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中新增 `ChevronIcon`，用于主图区域的左右切换箭头。
  - 将当前结果变更后的自动定位逻辑从 `scrollIntoView` 改为只驱动 `.job-workbench-browser-strip` 的横向滚动，不再改变整页垂直滚动位置。
  - 在主图区域外包 `job-preview-stage-media`，并增加左右透明切换按钮，复用现有 `selectPreviousItem / selectNextItem` 逻辑。
  - 保留主图点击放大预览功能，箭头切换与放大查看互不冲突。
  - 在 `app/theme-refactor.css` 中补齐主图浮层箭头的透明背景、悬停态、禁用态和窄屏尺寸规则。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前箭头是左右切换布局，和截图中的标注位置一致；如果后续明确希望改成上下箭头样式，可以在保留同一交互逻辑的前提下再做视觉替换。

## 2026-03-12 结果切换取消页面跟滚并补主图箭头验证
- 阶段目标：确认切换当前结果时页面不再自动跳动，并且主图两侧的透明箭头可直接切换结果。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 启动本地服务并访问 `http://127.0.0.1:3049/jobs/job_cffe61a2-1e6c-43ed-b6a7-ec1c2ef85350`。
  - 浏览器确认主图区域已出现左右透明切换按钮。
  - 将页面滚动到约 `1100px` 后执行主图右侧箭头切换，切换前后 `scrollY` 均为 `1100.0001220703125`，说明页面未被自动调节。
  - 同一次验证中确认当前结果标题从“多场景应用 · 1:1 · 4K · #1”切换为“细节图 · 1:1 · 4K · #1”，说明主图箭头切换已生效。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收时仍存在既有的 `favicon.ico 404`，与这次切换交互优化无直接关系。
## 2026-03-12 直驱复刻 Prompt 默认收起
- 阶段目标：让任务详情页右侧的“本次实际发送给模型的直驱复刻 prompt”在首次打开时默认收起，而不是一进页面就展开。
- 已完成事项：
  - 在 `components/job-details-client.tsx` 中移除了 `prompt-details-drawer` 的默认 `open` 状态。
  - 为该 `details` 节点增加了基于当前生成项的 `key`，保证切换到别的结果时也会重新以收起状态出现，不沿用上一个结果的展开状态。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前只调整了右侧 Prompt 抽屉的默认状态；如果后续希望把其它调试抽屉也统一成默认收起，需要再集中扫一轮详情页抽屉策略。

## 2026-03-12 直驱复刻 Prompt 默认收起验证
- 阶段目标：确认默认收起改动没有影响详情页构建与交互稳定性。
- 已完成事项：
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 确认 `prompt-details-drawer` 已不再使用默认 `open` 属性，改为首次渲染收起。
- 涉及文件：
  - `components/job-details-client.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 构建阶段仍会出现仓库既有的 `node:sqlite` experimental warning，与这次抽屉默认状态调整无直接关系。

## 2026-03-12 参考图模式预览完整显示修复
- 阶段目标：修复参考图复刻模式下左侧“图片原图 / 参考图”预览区域显示不完整的问题，让横图和竖图都能完整适配预览框。
- 已完成事项：
  - 在 `app/theme-refactor.css` 中重写 `.preview-stage` 和 `.preview-stage-image` 的尺寸规则，改为预览容器固定可视高度、图片使用 `max-width` / `max-height` + `object-fit: contain` 进行完整适配。
  - 保留预览框边框、背景和圆角样式，只调整容器与图片的布局方式，避免影响创作台其余视觉层级。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 使用浏览器访问 `http://127.0.0.1:3050/create`，切换到“参考图复刻”模式，并注入一张 `16:9` 横图和一张 `9:16` 竖图进行验证；确认原图预览与参考图预览都能在左侧卡片内完整显示，不再被裁切。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 这次验证基于浏览器内生成的测试图完成；如后续用户仍反馈个别真实素材显示异常，需要再补查是否存在极端长图或透明边素材带来的视觉误判。

## 2026-03-12 全站手绘风格主题重构
- 阶段目标：把当前偏科技玻璃质感的 UI 重构为统一的手绘风格，让首页、创作台、历史页、详情页、设置页和模板中心都具备纸张底、墨线边框和手写标注感。
- 已完成事项：
  - 在 `app/theme-refactor.css` 顶层 token 中将全局配色切换为暖纸底色，并重设蓝 / 绿 / 红三色为更像彩铅与马克笔的低饱和版本。
  - 重写 `body` 背景与叠层纹理，加入纸张颗粒、淡色笔触和浅色面光，移除原先偏科技网格的冷色氛围。
  - 新增手绘字体策略，统一将标题、标签、按钮、导航和状态胶囊切换为更接近手写笔记的字体栈与字距规则。
  - 将 `app-header`、`panel`、`page-kpi`、`stat-card`、`create-panel`、`settings-card`、`template-card`、`detail-kv-card`、`variant-browser-card` 等高频容器重构为纸卡片视觉，并补上虚线内描边与纸面阴影。
  - 统一导航、语言切换、主按钮、次按钮、chip、状态胶囊、详情页工具按钮和切换按钮的手绘描边、悬停位移与按压反馈。
  - 统一输入框、下拉框、文本域与上传控件外观；其中文件上传区补上 `file-selector-button` 的手绘按钮样式，避免创作台左侧仍保留系统控件观感。
  - 保持已有 8pt grid、Card 24 内边距、Card 间距 16、Button / Input 高度 40 等尺寸规范不变，仅替换视觉语言。
  - 执行 `npm run typecheck`，通过。
  - 执行两轮 `npm run build`，通过。
  - 使用浏览器分别验证 `http://127.0.0.1:3050/`、`http://127.0.0.1:3050/create`、`http://127.0.0.1:3050/history`、`http://127.0.0.1:3050/jobs/job_ef17edcd-63c9-413e-abfc-d9613d5abebc`、`http://127.0.0.1:3052/settings`、`http://127.0.0.1:3052/templates`，确认主要页面已统一成手绘风格；随后再用 `http://127.0.0.1:3054/create` 复验上传控件样式已生效。
- 涉及文件：
  - `app/theme-refactor.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 浏览器验收过程中仍存在仓库既有的 `favicon.ico 404`。
  - 设置页仍会出现既有的 `Multiple forms should be contained...` DOM 提示，这次主题重构没有处理其结构性问题。

## 2026-03-12 正式启动脚本 EBUSY 锁库修复
- 阶段目标：修复点击 `启动正式版.bat` 时构建阶段报 `EBUSY: resource busy or locked, unlink .next\\standalone\\data\\commerce-image-studio.sqlite` 的问题，避免残留 standalone 进程锁住构建产物数据库。
- 已完成事项：
  - 确认锁库根因是残留的 `node .next\\standalone\\server.js` 进程占用了 `.next\\standalone\\data\\commerce-image-studio.sqlite`，导致 `npm run build` 在刷新 standalone 目录时无法删除旧库文件。
  - 在 `lib/server-config.ts` 中新增 `COMMERCE_STUDIO_DATA_DIR` / `COMMERCE_STUDIO_STORAGE_DIR` / `COMMERCE_STUDIO_DB_PATH` 环境变量支持，让数据库和素材目录可以显式固定，不再完全依赖 `process.cwd()`。
  - 新增 `scripts/stop-project-standalone.ps1`，用于在构建前自动清理当前仓库下残留的 `.next\\standalone\\server.js` 进程；同时兼容绝对路径和相对路径两种启动命令匹配。
  - 新增 `scripts/start-project-standalone.ps1`，统一本地正式启动时的 standalone 启动逻辑，并显式注入 `PORT`、`HOSTNAME` 和 `COMMERCE_STUDIO_DATA_DIR`。
  - 更新 `启动正式版.bat`：
    - 构建前自动执行解锁脚本；
    - 启动前显式创建项目根目录 `data`；
    - 启动时不再走 `npm start`，改为使用 `node .next\\standalone\\server.js`；
    - 启动链路显式绑定根目录 `data`，避免把运行库落进 `.next\\standalone\\data`。
  - 更新 `scripts/package-release.ps1` 生成的发布版启动器，补上 `COMMERCE_STUDIO_DATA_DIR=%~dp0data`，让发布目录也固定使用自身 `data`。
  - 运行解锁脚本后，成功终止残留的 standalone 进程 `57416`。
  - 执行 `npm run build`，通过，确认锁库问题已解除。
- 涉及文件：
  - `lib/server-config.ts`
  - `scripts/stop-project-standalone.ps1`
  - `scripts/start-project-standalone.ps1`
  - `启动正式版.bat`
  - `scripts/package-release.ps1`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 本轮已验证解锁后 `npm run build` 可恢复正常；但由于批处理启动本身会后台拉起独立终端窗口，终端内未额外做整条双击链路的自动化冒烟，后续如需可再补一轮手工复验。
## 2026-03-12 正式启动链路二次修复与烟测
- 阶段目标：修复 `启动正式版.bat` 在构建成功后仍无法稳定拉起 standalone 服务的问题，并补上空路径容错与可读日志。
- 已完成事项：
  - 将 `启动正式版.bat` 的旧进程释放步骤前移，并把端口探测挪到构建完成后，避免旧进程影响端口选择。
  - 将正式启动改为调用 `scripts/start-project-standalone.ps1`，由脚本内部负责写入输出日志和错误日志，不再使用不稳定的 `start ... > logfile` 外层重定向。
  - 在 `scripts/start-project-standalone.ps1` 和 `scripts/stop-project-standalone.ps1` 中补上 `ProjectRoot` 为空时的自动回退，避免从非标准入口调起时因为空路径报错。
  - 在 `启动正式版.bat` 中新增 `SCRIPT_DIR` 兜底，统一用脚本目录生成 `data`、日志和脚本路径。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 使用 `cmd /c start ... powershell -File scripts/start-project-standalone.ps1` 做本地烟测，确认 `127.0.0.1:3000` 成功进入 `LISTENING`，standalone 服务可正常拉起。
- 涉及文件：
  - `启动正式版.bat`
  - `scripts/start-project-standalone.ps1`
  - `scripts/stop-project-standalone.ps1`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前错误日志里仍会看到仓库既有的 `node:sqlite` experimental warning，以及 PowerShell 对原生 stderr 的包装文本；它不会阻止服务启动，但后续如果要进一步提升日志观感，可以再改成纯 `cmd` 级日志落盘。
## 2026-03-12 安装 UI UX Pro Max 到项目本地 Codex Skills
- 阶段目标：按 `ui-ux-pro-max` 仓库作者对 Codex 的官方方式，把技能安装到当前项目 `.codex/skills/ui-ux-pro-max`，不影响全局 `C:\Users\AEboli\.codex\skills`。
- 已完成事项：
  - 复核当前项目本地 `.codex` 状态，确认此前仅存在环境配置，尚无 `.codex/skills` 目录，可直接按项目本地模式安装。
  - 校验安装前置环境：`node -v`、`npm -v`、`python --version` 均可正常执行。
  - 在项目根目录执行 `npx uipro-cli@2.2.3 init --ai codex`，成功生成本地技能目录 `.codex/skills/ui-ux-pro-max`。
  - 验收安装结果，确认以下文件与目录已生成：
    - `.codex/skills/ui-ux-pro-max/SKILL.md`
    - `.codex/skills/ui-ux-pro-max/scripts/search.py`
    - `.codex/skills/ui-ux-pro-max/data/`
  - 执行 `python .codex/skills/ui-ux-pro-max/scripts/search.py --help`，通过，确认脚本可正常运行，不是空壳安装。
  - 本次未新增 `Readme/` 使用说明或 PRD；本轮属于项目本地工具接入，不是对外协作交付模块。
- 涉及文件：
  - `.codex/skills/ui-ux-pro-max/SKILL.md`
  - `.codex/skills/ui-ux-pro-max/scripts/search.py`
  - `.codex/skills/ui-ux-pro-max/data/`
  - `doc/进展记录.md`
- 待办事项或风险：
  - Codex 需要重启后才能识别新安装的本地 skill；如重启前看不到 `ui-ux-pro-max`，属于预期现象。
  - 当前采用项目本地安装模式；如果后续希望跨项目复用，再考虑补装到全局 `C:\Users\AEboli\.codex\skills`。
## 2026-03-12 正式启动批处理尾斜杠传参修复
- 阶段目标：彻底修复 `启动正式版.bat` 因 `SCRIPT_DIR` 尾部反斜杠导致的 PowerShell 参数串坏问题，恢复双击正式版启动能力。
- 已完成事项：
  - 将 `启动正式版.bat` 中的 `SCRIPT_DIR` 改为通过 `for %%I in ("%~dp0.") do set "SCRIPT_DIR=%%~fI"` 生成，不再保留尾部反斜杠。
  - 将批处理里所有基于根目录的路径统一改为 `%SCRIPT_DIR%\\...` 拼接，包括 `scripts`、`.runtime`、`data`、日志文件和 `-ProjectRoot` / `-DataDir` 参数。
  - 在 `stop-project-standalone.ps1` 和 `start-project-standalone.ps1` 中新增路径规范化逻辑，对入参先做 `Trim()`、去首尾引号、空值回退，再执行 `GetFullPath()`。
  - 在启动日志中补记 `Project root`、`Data directory` 和 `Error log`，并在批处理失败输出中显示 `Script directory`。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，通过。
  - 执行 `cmd /d /c 'call "C:\\Users\\AEboli\\Documents\\Codex\\启动正式版.bat"'` 做真实链路回归，确认原先的“路径中具有非法字符”错误已消失，并成功拉起 `127.0.0.1:3000` standalone 服务。
- 涉及文件：
  - `启动正式版.bat`
  - `scripts/stop-project-standalone.ps1`
  - `scripts/start-project-standalone.ps1`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 `.runtime\\prod-3000.err.log` 中仍会记录 `node:sqlite` experimental warning，这属于既有运行时提示，不影响服务成功启动。
## 2026-03-12 UI UX Pro Max 全站界面重做与正式版静态资源修复
- 阶段目标：使用 `ui-ux-pro-max` 技能重做全站 UI，彻底退役旧主题；同时修复正式版 `standalone` 启动后 CSS / JS / 字体静态资源 404，避免页面退回成无样式文本。
- 已完成事项：
  - 使用 `C:\Users\AEboli\Documents\Codex\.codex\skills\ui-ux-pro-max\SKILL.md` 的检索流程，对电商 AI 出图工作台的设计系统、配色、字体和交互进行重新选型，最终确定为“premium editorial dark + soft glass”方向。
  - 在 `app/layout.tsx` 中切换全局字体为 `DM Sans + Playfair Display`，移除旧的 `theme-refactor.css` 引用，改为接入新的 `ui-ux-pro-max.css`。
  - 新建 `app/ui-ux-pro-max.css`，基于现有语义类名重做整套视觉系统，覆盖导航、首页 Hero、创作台、历史页、任务详情页、设置页、品牌库和模板中心。
  - 删除旧主题文件 `app/theme-refactor.css`，避免后续误接回旧界面。
  - 在浏览器中实际验收了正式版首页、创作页、历史页和任务详情页，截图输出到 `output/playwright/ui-overhaul-home.png`、`output/playwright/ui-overhaul-create.png`、`output/playwright/ui-overhaul-history.png`、`output/playwright/ui-overhaul-job-details.png`。
  - 排查出“界面没有 UI”的真实根因不是新样式失效，而是 `standalone` 本地启动时缺少 `.next/static`，导致 `/_next/static/...` 的 CSS、JS、字体全部 404。
  - 在 `scripts/start-project-standalone.ps1` 中新增运行前静态资源同步逻辑，会在每次正式版启动前自动把 `C:\Users\AEboli\Documents\Codex\.next\static` 同步到 `C:\Users\AEboli\Documents\Codex\.next\standalone\.next\static`；若未来存在 `public` 目录，也会一起同步。
  - 重新启动正式版后复验，`http://127.0.0.1:3061/` 可正常加载样式，Playwright 控制台 `error` 数量为 0，`.runtime/prod-3061.log` 中可看到 `Synced .next/static` 记录。
- 涉及文件：
  - `app/layout.tsx`
  - `app/ui-ux-pro-max.css`
  - `scripts/start-project-standalone.ps1`
  - `app/theme-refactor.css`（已删除）
  - `output/playwright/ui-overhaul-home.png`
  - `output/playwright/ui-overhaul-create.png`
  - `output/playwright/ui-overhaul-history.png`
  - `output/playwright/ui-overhaul-job-details.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - `node:sqlite` 的 experimental warning 仍会在构建与正式版日志中出现，但当前不影响 UI 加载与服务启动。
  - 创作页存在既有的“离开页面前草稿确认”逻辑，自动化浏览时会弹 `beforeunload` / `confirm`；用户手动使用不受影响，但后续若要做更顺畅的自动化回归，可以把这部分做成更细的脏状态判定。

## 2026-03-12 当前生图项目进度同步与状态校验
- 阶段目标：基于当前工作区未提交改动，重新同步“电商 AI 出图工作台”的实际推进状态，并补一轮最小可用性校验，确认现在离可继续迭代还差什么。
- 已完成事项：
  - 复核当前基线，确认最近已提交版本仍是 `b4debdb Release v0.3.1`，但工作区在此基础上还有一大批未提交改动，覆盖前端、生成链路、数据库、正式版启动脚本与对外文档。
  - 基于 `git diff --stat` 与关键文件差异，确认这批在做的工作主要集中在 4 条线：
    - 参考图复刻链路重做：`app/api/generate/route.ts`、`components/create-job-form.tsx`、`lib/job-builder.ts`、`lib/generation.ts`、`lib/templates.ts` 已收敛为“只取第一张参考图、只保留比例/分辨率/数量、严格以参考图为母版复刻”，并新增 `referenceCopyMode` 控制“完全参照原图 / 按文案表格”两种文案来源。
    - 提示词模式拆分控制：`components/create-job-form.tsx`、`lib/types.ts`、`lib/db.ts`、`lib/generation.ts` 已把“是否翻译到输出语言”和“是否继续优化为真实照片”拆成两个独立开关，不再强耦合。
    - 运行时恢复与历史能力增强：`app/layout.tsx`、`lib/runtime.ts`、`lib/queue.ts`、`lib/db.ts` 已补上启动即恢复队列、从数据库回收 `queued / processing` 任务、历史页汇总统计与分页；`scripts/start-project-standalone.ps1`、`scripts/stop-project-standalone.ps1`、`启动正式版.bat` 继续承担正式版启动与解锁职责。
    - 详情页与素材访问体验升级：`components/job-details-client.tsx`、`components/image-lightbox.tsx`、`app/api/assets/[assetId]/route.ts`、`lib/asset-url.ts`、`app/history/page.tsx` 已补上图片缩放/灯箱、按宽度压缩返回 WebP、历史页 Hero + 分页、以及只围绕纯图结果进行审核与导出。
  - 复核当前对外文档口径，确认 `README.md` 仍显示 `v0.3.0` 与旧安装器名，但 `package.json` 和 `Readme/版本说明-v0.3.1.md` 已是 `v0.3.1`，版本展示尚未完全收口。
  - 执行 `npm run typecheck`，通过。
  - 首次执行 `npm run build` 失败，报错 `EBUSY: resource busy or locked, rmdir .next\\standalone`；继续排查后确认是仍有 `node .next\\standalone\\server.js` 进程占用构建目录。
  - 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 成功停止残留正式版进程 `60808`。
  - 再次执行 `npm run build`，通过，说明当前代码本体可以正常构建，主要阻塞点仍是“构建前若已有正式版在跑，需要先解锁 standalone 目录”。
- 涉及文件：
  - `README.md`
  - `package.json`
  - `Readme/版本说明-v0.3.1.md`
  - `app/api/generate/route.ts`
  - `app/api/assets/[assetId]/route.ts`
  - `app/history/page.tsx`
  - `app/layout.tsx`
  - `components/create-job-form.tsx`
  - `components/job-details-client.tsx`
  - `components/image-lightbox.tsx`
  - `lib/asset-url.ts`
  - `lib/db.ts`
  - `lib/generation.ts`
  - `lib/job-builder.ts`
  - `lib/queue.ts`
  - `lib/runtime.ts`
  - `lib/templates.ts`
  - `scripts/start-project-standalone.ps1`
  - `scripts/stop-project-standalone.ps1`
  - `启动正式版.bat`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前仓库仍有大量未提交改动；如果要进入下一轮开发或准备发布，建议先决定是继续在这批工作区改动上推进，还是先拆成一轮可提交的阶段性版本。
  - `README.md` 与部分交付文档仍残留 `v0.3.0` 口径，和当前 `v0.3.1` / 工作区实际状态不一致，容易影响对外交付认知。
  - `npm run build` 在正式版运行中会继续撞上 standalone 目录锁；当前可通过 `scripts/stop-project-standalone.ps1` 规避，但如果后续频繁并行“运行正式版 + 本地构建”，仍建议把这条预处理固化到常用构建入口。
  - `node:sqlite` experimental warning 仍持续存在，当前不影响构建通过，但仍属于长期稳定性与发布可信度风险。
## 2026-03-12 首页/创建页/历史页/详情页六项 UI 修正落地（实现阶段）
- 阶段目标：统一修正首页小胶囊与紧凑卡片排版、创建页分辨率与内置模块布局、历史页缩略图与灯箱、以及详情页结果工作台头部布局。
- 已完成事项：
  - 重写 `components/image-lightbox.tsx`，把原来的单图灯箱升级为支持图片列表、前后切换、键盘左右键、透明悬浮箭头和底部动作按钮的新图库灯箱。
  - 更新 `components/job-table.tsx`，历史页预览列改为最多展示 4 张缩略图的 2x2 紧凑网格；灯箱现在会携带 `jobId + jobItemId`，底部“查看”按钮可跳转到 `/jobs/{id}?itemId=...`。
  - 更新 `app/jobs/[id]/page.tsx`，详情页路由同时兼容旧的 `?item=` 和新的 `?itemId=`，保证历史页灯箱跳转后能直接聚焦对应图片。
  - 更新 `components/job-details-client.tsx`，详情页主图、对比图和结果列表统一接入新的图库灯箱；灯箱内部支持前后切换，底部动作按钮直接打开当前原图链接，避免在详情页里自跳自己。
  - 在 `app/ui-ux-pro-max.css` 中补充覆盖样式，统一修正 KPI 卡片、快捷入口、小胶囊、历史缩略图网格、灯箱、详情页工作台头部，以及创建页 `is-compact-viewport` 下的局部滚动锁高问题。
  - 执行 `npm run typecheck`，通过。
- 涉及文件：
  - `components/image-lightbox.tsx`
  - `components/job-table.tsx`
  - `components/job-details-client.tsx`
  - `app/jobs/[id]/page.tsx`
  - `app/ui-ux-pro-max.css`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 还需继续执行 `npm run build` 与浏览器回归，确认新的灯箱行为、历史缩略图布局、创建页无局部滚动和详情页头部排版在真实页面里都符合预期。
  - `components/job-details-client.tsx` 仍保留部分历史中文文案编码风格混用，本轮没有顺手清理文本常量，只处理了行为与布局。
## 2026-03-12 首页/创建页/历史页/详情页六项 UI 修正落地（验证阶段）
- 阶段目标：完成本轮 UI 六项修正后的构建验证与真实页面回归，确认首页、创建页、历史页、灯箱和详情页都按预期工作。
- 已完成事项：
  - 执行 `npm run build` 首次仍命中既有的 `.next\\standalone` 锁目录问题；随后执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 停掉残留正式版进程后，再次执行 `npm run build`，通过。
  - 使用浏览器回归本地 `http://127.0.0.1:3054/`、`/create`、`/history` 与 `/jobs/job_f84d0aab-233a-43e7-9876-2a1a73c08bd3?itemId=item_0f748da8-a060-4a8b-8e97-5db2d0a56b85`，确认：
    - 首页 KPI 卡片、快捷入口和最近任务状态胶囊已居中排版，桌面端短文案保持单行；
    - 创建页分辨率区实际为 4 列，`create-main` 与 `create-sidebar-sticky` 的 `overflow` 已恢复为 `visible`、`max-height` 为 `none`，不再局部滚动；
    - 历史页首行缩略图网格实际为 `64px 64px` 双列，检测结果 `previewOverlap = false`，不再叠加；
    - 历史页灯箱成功显示左右透明箭头与底部“查看”按钮，且“查看”可跳转到带 `itemId` 的详情页链接；
    - 详情页 `job-workbench-hero` 已成为双列均衡布局，左右两块高度对齐。
  - 保存本轮回归截图：
    - `output/playwright/ui-fix-home-mcp.png`
    - `output/playwright/ui-fix-create-mcp.png`
    - `output/playwright/ui-fix-history-lightbox-mcp.png`
    - `output/playwright/ui-fix-job-details-mcp.png`
- 涉及文件：
  - `output/playwright/ui-fix-home-mcp.png`
  - `output/playwright/ui-fix-create-mcp.png`
  - `output/playwright/ui-fix-history-lightbox-mcp.png`
  - `output/playwright/ui-fix-job-details-mcp.png`
  - `scripts/stop-project-standalone.ps1`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 构建前若有正式版进程正在占用 `.next\\standalone`，仍需先执行解锁脚本；这次属于既有环境问题，不是本轮 UI 改动引入的新回归。
  - 浏览器控制台里依旧可见仓库已有的 `favicon.ico 404` 和 `node:sqlite` experimental warning，本轮未处理这两项历史问题。
## 2026-03-13 生图项目冗余文件清理与历史资料归档
- 阶段目标：根据前一轮冗余文件审计结果，清理当前运行与构建链路都不再使用的旧代码和供应商文件，并把不参与项目运行与交付的历史资料移出主目录。
- 已完成事项：
  - 删除未被引用的旧组件 `components/template-grid.tsx`。
  - 删除未被引用的旧海报生成器 `lib/poster.ts`。
  - 删除整组未再使用的 `medium-zoom` 供应商文件：`lib/vendor/medium-zoom.js`、`lib/vendor/medium-zoom.d.ts`、`lib/vendor/medium-zoom.LICENSE.txt`。
  - 清理 `app/globals.css` 中对应的 `.medium-zoom-*` 死样式，避免保留无效主题规则。
  - 将旧版本说明 `Readme/版本说明-v0.3.0.md` 归档到 `doc/archive/2026-03-13-历史遗留资料/Readme/版本说明-v0.3.0.md`。
  - 将两份横向研究资料从 `docs/research/` 迁移到 `doc/archive/2026-03-13-历史遗留资料/docs/research/`，避免继续混在当前生图项目资料目录中。
  - 执行 `rg -n "medium-zoom|TemplateGrid|createPosterSvg" -S app components lib README.md Readme docs scripts package.json`，确认当前项目源码与主文档入口已无这批符号残留命中。
  - 执行 `npm run typecheck`，通过。
  - 首次执行 `npm run build` 命中既有 `.next\\standalone` 占用问题；随后执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 停止残留正式版进程后，再次执行 `npm run build`，通过。
- 涉及文件：
  - `app/globals.css`
  - `components/template-grid.tsx`（已删除）
  - `lib/poster.ts`（已删除）
  - `lib/vendor/medium-zoom.js`（已删除）
  - `lib/vendor/medium-zoom.d.ts`（已删除）
  - `lib/vendor/medium-zoom.LICENSE.txt`（已删除）
  - `Readme/版本说明-v0.3.0.md`（已迁移归档）
  - `docs/research/amazon-us-electronic-fishing-lure-2026-03-09.md`（已迁移归档）
  - `docs/research/amazon-us-electronic-fishing-lure-2026-03-10-skill-addendum.md`（已迁移归档）
  - `doc/archive/2026-03-13-历史遗留资料/`
  - `doc/进展记录.md`
- 待办事项或风险：
  - `docs/research` 空目录仍在工作区，当前不影响运行与构建；如后续继续做仓库洁净度整理，可以一并移除空目录。
  - 构建前若正式版进程仍在占用 `.next\\standalone`，依旧需要先执行 `scripts/stop-project-standalone.ps1` 解锁；这是既有环境问题，不是本轮清理引入的新问题。
  - `node:sqlite` experimental warning 在构建日志中仍然存在，本轮未处理。
## 2026-03-13 创建页头部与创作模式布局优化，顺带清理旧 UI 残留
- 阶段目标：删除创建页相关的旧 chip/grid UI 残留，避免 `globals.css` 与 `ui-ux-pro-max.css` 双份样式叠加；同时修正创建页头部右侧信息卡显示不佳，以及“创作模式”保持桌面端一行 5 个选项。
- 已完成事项：
  - 将 `components/create-job-form.tsx` 中基础信息里的创作模式选择区改为独立 `creation-mode-grid`，桌面端固定 5 列，保证 5 个选项同一行显示。
  - 调整 `app/ui-ux-pro-max.css` 中创建页 Hero 区专属样式：右侧 `page-kpi-grid` 从通用 4 列改为创建页专属 3 列，3 张信息卡完整铺满可用宽度，不再留出空白列。
  - 优化创建页 Hero 右侧信息卡的排版：压缩文案、放大可用宽度、调整描述换行与卡片内部节奏，改善 Batch / Studio 这类长词和中文说明的观感。
  - 在 `app/ui-ux-pro-max.css` 中补充 `accordion-actions` 与 `accordion-toggle-button` 的当前主题样式归属，减少旧全局样式参与创建页头部布局。
  - 删除 `app/globals.css` 中整块旧版 `.chip-grid`、`.chip-grid-types`、`.chip-grid-ratios`、`.chip-grid-resolutions`、`.chip` 及其响应式断点规则，统一由 `app/ui-ux-pro-max.css` 接管这套 UI，降低屎山叠加风险。
  - 微调 `app/create/page.tsx` 的 3 条头部摘要文案，缩短中文与英文描述，适配新的信息卡布局。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`；首次仍命中既有 `.next\\standalone` 占用问题，执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 解锁后再次构建，通过。
  - 本地启动 `http://127.0.0.1:3056/create` 做浏览器实机回归，确认：
    - 创建页头部右侧 3 张卡片已铺满右侧区域；
    - 创作模式 5 个选项在桌面端保持单行；
    - Playwright 计算结果 `uniqueRows = 1`，说明创作模式确实未换行。
  - 保存回归截图：`output/playwright/create-ui-cleanup-2026-03-13.png`。
- 涉及文件：
  - `app/create/page.tsx`
  - `components/create-job-form.tsx`
  - `app/ui-ux-pro-max.css`
  - `app/globals.css`
  - `output/playwright/create-ui-cleanup-2026-03-13.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - `globals.css` 里仍保留其他历史 UI 规则，当前只清掉了已被新主题完全接管、且和创建页最容易打架的 chip/grid 旧样式；后续如继续做 UI 减负，建议按页面分批迁出或删除。
  - 构建前若正式版或本地预览进程继续占用 `.next\\standalone`，仍需先执行 `scripts/stop-project-standalone.ps1` 解锁；这是仓库既有环境问题，不是本轮改动引入的新问题。
  - `node:sqlite` experimental warning 在构建日志中仍然存在，本轮未处理。
## 2026-03-13 胶囊文字收口与同类卡片文案对齐优化
- 阶段目标：确保胶囊与紧凑卡片中的文字不超出容器边界，并把同一类信息卡的标题、数值、说明文案做统一的行数与高度对齐，提升整体观感。
- 已完成事项：
  - 调整 `app/ui-ux-pro-max.css` 中创建页“创作模式”胶囊的文字容纳规则，给胶囊内容补充固定最小文字区高度、2 行截断与安全换行，避免“亚马逊A+图”这类较长文案贴边或溢出。
  - 对比例/分辨率胶囊单独保留单行样式，避免通用双行规则影响参数区紧凑布局。
  - 统一 `page-kpi`、`stat-card`、`quick-link-card`、`hero-feature-card` 这类紧凑卡片的标题与描述区高度，使同类卡片的行数、留白和视觉基线保持一致。
  - 优化首页最近任务卡片标题区域，限制长标题最多显示 2 行，避免文本把按钮区和统计胶囊顶乱。
  - 通过浏览器实测确认 5 个创作模式胶囊均未出现横向或纵向溢出；模板中心 KPI 卡片高度、标题区高度与描述区高度均保持一致。
- 涉及文件：
  - `app/ui-ux-pro-max.css`
  - `output/playwright/create-chip-alignment-2026-03-13.png`
  - `output/playwright/template-kpi-alignment-2026-03-13.png`
  - `output/playwright/overview-title-clamp-2026-03-13.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前这轮主要统一了胶囊、KPI 卡片、快捷入口卡片和最近任务卡片；仓库里如果后续新增新的“短标题 + 短说明”卡片，建议继续复用这套统一行高规则，避免再次出现各卡片行数不齐。
  - 长标题目前采用截断策略保证不撑坏布局；如果后续希望在悬浮时展示完整文案，可以再补 tooltip 或详情入口，但本轮未扩展交互。
## 2026-03-13 设置页 Hero 信息卡布局补齐
- 阶段目标：修正设置页顶部 Hero 右侧 3 张信息卡仍沿用通用 4 列布局导致留白过大、卡片过窄的问题，并统一卡片内部文案节奏。
- 已完成事项：
  - 在 `app/settings/page.tsx` 中为设置页 Hero 增加专属类名 `settings-page-intro`，不再与其他页面共用同一套顶部信息卡布局。
  - 调整设置页 Hero 的 3 张信息卡文案，压缩描述长度，减少无意义换行与空白。
  - 在 `app/ui-ux-pro-max.css` 中新增设置页专属 Hero 样式：右侧信息卡改为 3 列铺满、统一卡片最小高度、统一标题与描述区高度和留白。
  - 补充设置页信息卡主词的最小显示高度与行高，降低不同机器字体渲染时贴边的风险。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`，首次命中既有 `.next\\standalone` 锁目录问题；执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 解锁后重建通过。
  - 使用浏览器回归 `http://127.0.0.1:3057/settings`，确认设置页 Hero 右侧信息卡已为 3 列等宽布局，3 张卡高度一致，描述区高度一致，不再出现 4 列栅格留下的大块空白。
  - 保存回归截图：`output/playwright/settings-hero-kpi-2026-03-13-final.png`。
- 涉及文件：
  - `app/settings/page.tsx`
  - `app/ui-ux-pro-max.css`
  - `output/playwright/settings-hero-kpi-2026-03-13-final.png`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 设置页主体表单区仍保留较长的说明卡与输入区组合，当前这轮只修正了顶部 Hero 信息卡；如果后续要继续压缩设置页首屏高度，可以再拆一轮针对表单卡片标题区和说明卡的排版优化。
  - 构建前若已有 standalone 进程占用 `.next\\standalone`，仍需要先执行 `scripts/stop-project-standalone.ps1` 解锁；这是仓库现存环境问题，不是本轮改动引入。
## 2026-03-13 版本 0.4.0 发版准备与对外说明整理
- 阶段目标：将当前版本整理为 `v0.4.0` 的对外发布口径，补齐 GitHub 仓库发版所需的版本号与版本说明，并确保内部进展记录不进入上传内容。
- 已完成事项：
  - 将 `package.json` 与 `package-lock.json` 的版本号从 `0.3.1` 更新为 `0.4.0`。
  - 更新 `README.md` 中的版本号、安装器命名与 GitHub Releases / Source Code 链接，统一到当前仓库 `aEboli/gemini-commerce-image-workbench`。
  - 重写 `README.md` 中的 “What’s New” 区块，按 `v0.4.0` 的对外能力口径描述，不使用内部修改记录格式。
  - 更新 `Readme/使用说明-电商AI出图工作台.md` 中的安装器名称为 `IMAGE-STUDIO-WINDOWS-0.4.0.exe`。
  - 新增 `Readme/版本说明-v0.4.0.md`，作为本次 GitHub 发版的正式版本说明，内容聚焦版本能力与适用场景，不包含内部修改记录。
- 涉及文件：
  - `package.json`
  - `package-lock.json`
  - `README.md`
  - `Readme/使用说明-电商AI出图工作台.md`
  - `Readme/版本说明-v0.4.0.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 `gh auth status` 显示本机未登录 GitHub CLI；若后续需要创建 GitHub Release 页面，将优先尝试现有 git 推送能力，CLI 级 Release 创建可能受限。
  - 本地 `doc/` 为内部进展记录目录，本轮会继续保留本地记录，但不会加入 GitHub 上传清单。
## 2026-03-13 v0.4.0 仓库发布完成
- 阶段目标：将 `v0.4.0` 的代码与版本标签安全发布到 GitHub 仓库，同时排除内部进展记录目录。
- 已完成事项：
  - 将本次发布内容提交为 `Release v0.4.0`。
  - 创建并校正 `v0.4.0` Git 标签，使其指向变基后的最终发布提交。
  - 先拉取远端最新 `origin/main`，吸收远端新增的 `Delete Readme/目录说明.md` 提交后完成变基，避免覆盖远端更新。
  - 成功推送 `main` 到 `origin/main`。
  - 成功推送并更新远端标签 `v0.4.0`。
  - 全程未将 `doc/` 目录加入提交或上传内容，满足“不上传修改记录”的要求。
- 涉及文件或对象：
  - Git commit: `8e5f826` (`Release v0.4.0`)
  - Git tag: `v0.4.0`
  - 远端仓库：`https://github.com/aEboli/gemini-commerce-image-workbench`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前本机 `gh auth status` 仍显示未登录，因此这次完成的是“代码与标签发布到 GitHub 仓库”；若后续需要自动创建 GitHub Release 页面，需要先登录 GitHub CLI 或提供可用的发布凭据。
## 2026-03-13 GitHub Release 页面创建受阻
- 阶段目标：用 GitHub CLI 为已推送的 `v0.4.0` 标签创建 GitHub Release 页面，并使用 `Readme/版本说明-v0.4.0.md` 作为正式版本说明。
- 已完成事项：
  - 复核当前终端会话中的 `gh auth status`。
  - 复核 `gh release list` 调用可用性。
  - 确认 `Readme/版本说明-v0.4.0.md` 已可直接作为发布说明正文使用。
- 涉及文件或对象：
  - `Readme/版本说明-v0.4.0.md`
  - GitHub CLI (`gh`)
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前终端中的 `gh` 仍返回 “You are not logged into any GitHub hosts.”，说明本会话尚未拿到 GitHub CLI 的有效凭据；在凭据可用前，无法自动创建 GitHub Release 页面。
  - 代码提交与标签 `v0.4.0` 已经推送到仓库，当前只差 Release 页面这一步。
## 2026-03-13 GitHub Release 页面创建完成
- 阶段目标：为已发布到仓库的 `v0.4.0` 标签创建正式 GitHub Release 页面，并使用对外版本说明作为发布正文。
- 已完成事项：
  - 确认当前终端中的 `gh` 已登录 GitHub 账号 `aEboli`。
  - 确认仓库中此前不存在 `v0.4.0` 的 Release 页面。
  - 使用 `Readme/版本说明-v0.4.0.md` 作为正文，执行 `gh release create v0.4.0 --title "v0.4.0" --notes-file "Readme/版本说明-v0.4.0.md"` 创建正式 Release 页面。
  - 创建成功，Release 地址为：`https://github.com/aEboli/gemini-commerce-image-workbench/releases/tag/v0.4.0`。
  - 发布正文使用对外版本说明，不包含内部修改记录。
- 涉及文件或对象：
  - `Readme/版本说明-v0.4.0.md`
  - GitHub Release: `v0.4.0`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 Release 页面尚未附带安装包等二进制附件；如后续需要，可再上传 `IMAGE-STUDIO-WINDOWS-0.4.0.exe` 或安全发布压缩包。
## 2026-03-13 安装器上传与仓库 About 完成
- 阶段目标：为 `v0.4.0` Release 上传可下载安装器，并补齐 GitHub 仓库 About 信息，方便外部访问者理解项目定位与下载入口。
- 已完成事项：
  - 执行 `npm run package:installer:exe:safe`，生成安全发布版安装器 `release/IMAGE-STUDIO-WINDOWS-0.4.0.exe`。
  - 通过 `gh release upload v0.4.0 "release/IMAGE-STUDIO-WINDOWS-0.4.0.exe"` 将安装器上传到 `v0.4.0` Release。
  - 复核 Release 资产列表，确认安装器已成功上传，可公开下载。
  - 使用 `gh repo edit` 为仓库设置 About 描述、Releases 主页链接，并补充 `gemini`、`ecommerce`、`nextjs`、`feishu`、`image-generation` 等 topics。
- 涉及文件或对象：
  - `release/IMAGE-STUDIO-WINDOWS-0.4.0.exe`
  - GitHub Release `v0.4.0`
  - GitHub 仓库 About 配置
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 Release 只上传了安装器 EXE；如后续还需要安全压缩包、绿色版目录包或校验文件，可继续补传到同一 Release。
  - 本地 `release/` 目录为构建产物目录，不会进入源码仓库；如需长期归档安装器，建议以 GitHub Releases 为主。

## 2026-03-13 全站原生下拉深色化、数量输入放宽到 10，并补充新平台与双制尺寸转换
- 阶段目标：统一修复深色主题下原生下拉框展开面板的可读性问题；把创建页“每种类型生成数量”改成支持 `1-10` 且可临时删空；补充拼多多、Temu、SHEIN 平台；并在所有模式识别到单制尺寸/重量时自动转换为公制+英制双制表达，重点覆盖亚马逊 A+ 图与提示词模式链路。
- 已完成事项：
  - 在 `app/ui-ux-pro-max.css` 和 `app/globals.css` 中统一补充原生 `select` / `option` / `optgroup` 的深色规则，包含 `color-scheme: dark`、深色选项背景、浅色文字、禁用态颜色和选中态颜色，避免出现白底浅字或白底蓝选中条。
  - 在 `components/create-job-form.tsx` 中把数量输入改为“字符串编辑态 + 合法数值态”双层逻辑：
    - 允许从 `1` 删到空，不会立即回弹。
    - 合法范围扩展到 `1-10`。
    - 空值、`0`、`11`、非整数等都会显示字段级错误，并阻止提交。
    - 请求总数摘要在数量无效时不再沿用旧值，改为“待填写 / 必填后计算”提示。
  - 在 `app/api/generate/route.ts` 中补充服务端校验，要求 `variantsPerType` 必须是 `1-10` 的整数；即使前端被绕过，后端仍会返回 `400`。
  - 在 `lib/constants.ts` 中补充平台选项：`拼多多`、`Temu`、`SHEIN`。
  - 在 `lib/templates.ts` 中补充对应平台的视觉风格描述，并新增尺寸/重量双制归一化工具：
    - 识别输入中是否只包含单一计量体系。
    - 若只有公制或英制，则自动生成双制参考表达。
    - 所有模式在检测到尺寸信息时，都能拿到双制后的规范化文本。
  - 在 `lib/templates.ts` 中把尺寸/重量规则接入：
    - 标准出图文案提示词。
    - 图片生成提示词。
    - 提示词模式提示词。
    - 非尺寸图若偶发出现尺寸，也要求保持双制，不让其成为主叙事。
  - 在 `lib/gemini.ts` 中把尺寸双制规则接入翻译与提示词优化链路：
    - 输入翻译时，如果只有单制，要求补出另一制并保留原始主制顺序。
    - 真实照片优化提示词时，若出现尺寸/重量，也要求保持双制。
    - 提示词模式的生成调用现在会把 `materialInfo` 和 `sizeInfo` 一并传入，避免提示词模式漏掉尺寸规则。
  - 通过浏览器实测验证：
    - 创建页、历史页、模板中心原生下拉已是深色展开面板。
    - 创建页平台下拉已出现 Amazon、TikTok Shop、淘宝、天猫、京东、拼多多、Temu、SHEIN、Shopee、Lazada、eBay、Etsy、Rakuten、AliExpress。
    - 创建页数量输入可删空、可输入 `10`、输入 `11` 会报错且不允许提交。
    - 设置页当前可见区域没有原生 `select`，因此不存在旧白底下拉残留。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`；首次仍命中既有 `.next\\standalone` 占用问题，执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 解锁后再次构建，通过。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `app/ui-ux-pro-max.css`
  - `app/globals.css`
  - `app/api/generate/route.ts`
  - `lib/constants.ts`
  - `lib/templates.ts`
  - `lib/gemini.ts`
  - `doc/进展记录.md`
- 待办事项或风险：
  - Windows 原生下拉面板的最终视觉仍受浏览器与系统控件渲染影响；当前已把主题方向统一到深色，但若后续希望获得完全一致的悬浮面板外观，仍需单独换成自定义下拉组件。
  - 当前“临时空值”只存在于前端编辑态，不会落库也不会发给后端；刷新页面后会恢复到最近一次有效数量或默认值 `1`，这是有意保持的安全行为。
  - 双制转换目前优先覆盖常见尺寸/重量单位（如 `cm / mm / m / kg / g / in / ft / lb / oz`）；如后续需要支持更复杂的体积、容量或混合规格表达，可继续扩展单位映射。

## 2026-03-13 自动翻译漏翻补充说明修复
- 阶段目标：排查“原始输入 vs 自动翻译后输入”里补充说明仍保留原文，导致最终生成图里也混入未翻译文本的问题，并修复翻译链路的漏翻容错。
- 已完成事项：
  - 复核 `lib/generation.ts`、`lib/gemini.ts`、`components/job-details-client.tsx` 的链路，确认问题不在详情页展示，而在 `translateCreativeInputs()` 的首轮结构化翻译里，`sourceDescription` 这类多行字段可能被模型原样返回，后续又因为兜底逻辑直接回退原文，导致生成 prompt 继续吃到中文。
  - 在 `lib/gemini.ts` 中新增“漏翻检测”规则：
    - 当目标语言为非中日韩语言时，如果字段结果仍包含汉字，则判定为疑似未翻译。
    - 当目标语言为中文时，如果字段完全保留原英文且未出现中文，也会触发补翻。
    - 重点覆盖 `productName`、`sellingPoints`、`restrictions`、`sourceDescription`、`materialInfo`、`sizeInfo`。
  - 将漏翻补救从原来的“整批字段二次翻译”收紧为“逐字段、逐行翻译”：
    - 对命中的漏翻字段按行拆分。
    - 要求模型按原顺序返回同样数量的行，禁止合并多行。
    - 保留行结构、计量单位和量纲顺序。
    - 尺寸/重量行若只有单制，仍要求补成双制。
  - 保留原有首轮结构化翻译能力，但在返回前对疑似漏翻字段自动触发更严格的逐行补翻，避免 `补充说明`、`材质`、`尺寸` 这种混合文本继续原样透传到出图 prompt。
  - 用真实接口做回归验证，输入样例包含中文标题、卖点和多行补充说明后，输出结果确认：
    - `productName` 已翻译为英文。
    - `sellingPoints` 已翻译为英文。
    - `sourceDescription` 先前会保留中文，现在已翻译为英文多行文本。
    - 尺寸/重量行会补出英制换算，例如 `12cm` -> `12cm (4.72in)`、`18g` -> `18g (0.63oz)`。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`；首次仍命中既有 `.next\\standalone` 占用问题，执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 解锁后再次构建，通过。
- 涉及文件：
  - `lib/gemini.ts`
  - `lib/generation.ts`
  - `components/job-details-client.tsx`
  - `output/translation-check.json`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 这次已实测修复中文 -> 英文的漏翻场景；如后续还有英文 -> 中文、或日文/韩文市场的类似投诉，建议再补一轮多语种样本回归。
  - 当前逐行补翻会增加少量翻译调用成本，但只在首轮结果疑似漏翻时触发，平时不会全量放大请求。

## 2026-03-13 创作页头部同步摘要收口
- 阶段目标：减少创作页表单区头部被“同步显示的输入摘要”占用的垂直空间，去掉基础信息区的同步摘要，并让参数设置区只显示真实已选中的参数，不再显示无意义占位。
- 已完成事项：
  - 在 `components/create-job-form.tsx` 中移除了“基础信息”折叠面板头部的同步摘要行，避免提示词模式下把输入框内容直接顶到标题下面，占用首屏空间。
  - 重新整理“参数设置”折叠面板头部的摘要拼接逻辑，改为只拼接当前真实已选中的项。
  - 参数摘要不再使用 `-` 作为未选占位，也不再显示 `0 类图` 这类无效信息。
  - 保留模式、国家、平台、语言、比例、分辨率等已选且当前场景有效的信息，让摘要仍然有信息密度，但不会制造噪音。
  - 通过浏览器回归确认：
    - “基础信息”头部已不再出现同步输入内容。
    - 提示词模式下“参数设置”头部摘要为 `美国 · Amazon · 英语（美国） · 提示词模式 · 1:1 · 4K`。
    - 页面中不存在 `-` 占位或 `0 类图` 这类未选择参数的展示。
  - 执行 `npm run typecheck`，通过。
  - 执行 `npm run build`；首次仍命中既有 `.next\\standalone` 占用问题，执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\stop-project-standalone.ps1 -ProjectRoot .` 解锁后再次构建，通过。
- 涉及文件：
  - `components/create-job-form.tsx`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前只收掉了“基础信息”头部摘要，`高级选项` 仍保留简短状态摘要；如果后续还想继续压缩首屏，可以再评估是否把高级区摘要也改成更轻量的徽标状态。
  - 参数摘要仍属于“已选状态回显”，后续如果参数种类继续增加，建议改成更结构化的 tag 组，而不是继续线性拼接字符串。

## 2026-03-13 PRD-Creater Skill 需求梳理与官方模板研究
- 阶段目标：明确 `PRD-Creater` skill 的触发方式、交付形态与 Notion 官方 PRD 模板骨架，为后续技能落地提供稳定依据。
- 已完成事项：
  - 读取 `$skill-creator` 的完整规范，确认新 skill 需要遵循 hyphen-case 命名、精简 frontmatter、`agents/openai.yaml` 元数据和 `quick_validate.py` 校验流程。
  - 复核当前本地 skill 目录结构与现有 `agents/openai.yaml` 示例，确认新 skill 适合落在 `C:\\Users\\AEboli\\.codex\\skills\\prd-creater`。
  - 核对 Notion 官方公开资料，整理出可复用的 PRD 核心骨架：`Context`、`Goals and KPIs`、`Constraints and assumptions`、`Dependencies`、`Tasks`，并补充官方博客中强调的 `scope`、`features`、`release criteria`、`metrics` 等写作要点。
- 涉及文件：
  - `C:\\Users\\AEboli\\.codex\\skills\\.system\\skill-creator\\SKILL.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\.system\\skill-creator\\references\\openai_yaml.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\.system\\skill-creator\\scripts\\init_skill.py`
  - `C:\\Users\\AEboli\\.codex\\skills\\.system\\skill-creator\\scripts\\generate_openai_yaml.py`
  - `C:\\Users\\AEboli\\.codex\\skills\\.system\\skill-creator\\scripts\\quick_validate.py`
  - `doc/进展记录.md`
- 待办事项或风险：
  - Notion Marketplace 搜索页本身更偏模板发现入口，官方公开资料能确认的是核心结构与写作原则；具体模板字段需要在 skill 内做稳定化抽象，避免过度依赖页面瞬时展示。
  - 用户给出的名称是 `PRD-Creater`，但 skill 文件夹需遵循 `hyphen-case`，因此落地时需要规范为 `prd-creater`，并在文档中说明映射关系。

## 2026-03-13 PRD-Creater Skill 落地、文档交付与校验完成
- 阶段目标：将 `PRD-Creater` 正式落地为可复用 skill，补齐参考模板、协作文档与验收校验。
- 已完成事项：
  - 使用 `init_skill.py` 在 `C:\\Users\\AEboli\\.codex\\skills\\prd-creater` 初始化 skill 骨架，并生成 `agents/openai.yaml`。
  - 完成 `SKILL.md`，明确 skill 的适用场景、工作流、输出规则、质量标准和资源引用方式。
  - 新增 `references/notion-official-prd-template.md`，沉淀基于 Notion 官方帮助中心、官方博客与官方模板页整理出的稳定 PRD 结构。
  - 新增 `assets/notion-prd-template.md`，提供可直接复用的 Notion 风格 Markdown PRD 模板。
  - 在 `Readme/` 下新增 `使用说明-PRD-Creater.md` 与 `PRD-PRD-Creater.md`，分别说明使用方式和该 skill 的产品定义，满足协作者交付要求。
  - 处理 Windows 默认编码下 `quick_validate.py` 读取 `SKILL.md` 失败的问题：将 `SKILL.md` 收敛为 ASCII 兼容内容后重新校验，最终通过。
  - 执行校验命令并通过：`python C:\\Users\\AEboli\\.codex\\skills\\.system\\skill-creator\\scripts\\quick_validate.py C:\\Users\\AEboli\\.codex\\skills\\prd-creater`
- 涉及文件：
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\SKILL.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\agents\\openai.yaml`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\references\\notion-official-prd-template.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\assets\\notion-prd-template.md`
  - `Readme/使用说明-PRD-Creater.md`
  - `Readme/PRD-PRD-Creater.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 当前 skill 已完成结构与校验，但尚未做真实业务需求的前向试跑；如后续要进一步提高稳定性，建议用一条真实功能需求做一次完整生成回归。
  - 如果 Notion 官方 PRD 模板未来新增稳定字段，优先同步更新 `references/notion-official-prd-template.md` 与 `assets/notion-prd-template.md`，避免 `SKILL.md` 变得过重。

## 2026-03-13 PRD-Creater 接入 Notion Marketplace 全量 PRD 模板目录
- 阶段目标：把 `PRD-Creater` 从“单一通用模板”升级为“基于 Notion Marketplace 全量 PRD 搜索结果驱动”的 skill，完成用户要求的“全部都扒”。
- 已完成事项：
  - 发现 Notion Marketplace 搜索页 `https://www.notion.com/templates/search?query=PRD` 的公开 `__NEXT_DATA__` 中直接包含模板列表和统计字段，无需依赖登录态即可同步目录数据。
  - 新增脚本 `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\scripts\\sync_notion_marketplace_catalog.py`，可自动抓取 Marketplace 搜索页、解析模板数据并生成目录快照。
  - 执行同步脚本后，成功落盘当前 `PRD` 搜索快照：
    - 总模板数：`98`
    - 免费模板：`68`
    - 付费模板：`30`
    - Notion 官方模板：`6`
  - 新增 `references/notion-prd-marketplace-catalog.json`，保存 98 条模板的结构化目录，包括名称、slug、链接、价格、创作者、分类、正文提取文本等字段。
  - 新增 `references/notion-prd-marketplace-catalog.md`，生成人可浏览的 98 条模板索引表。
  - 新增 `references/notion-prd-marketplace-selection-guide.md`，把目录映射成可执行的模板选型规则。
  - 更新 `SKILL.md`，让 skill 在起草 PRD 前先走模板目录筛选，并支持“匹配模板 / 对比模板 / 短名单推荐 / 指定官方模板 / 指定免费模板”等用法。
  - 更新 `agents/openai.yaml`、`Readme/使用说明-PRD-Creater.md`、`Readme/PRD-PRD-Creater.md`，同步新的目录驱动能力与当前快照范围。
  - 重新执行 `quick_validate.py` 并通过，确认 skill 在升级后仍符合规范。
- 涉及文件：
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\scripts\\sync_notion_marketplace_catalog.py`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\references\\notion-prd-marketplace-catalog.json`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\references\\notion-prd-marketplace-catalog.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\references\\notion-prd-marketplace-selection-guide.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\SKILL.md`
  - `C:\\Users\\AEboli\\.codex\\skills\\prd-creater\\agents\\openai.yaml`
  - `Readme/使用说明-PRD-Creater.md`
  - `Readme/PRD-PRD-Creater.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - `98` 是本次同步时的快照值，后续随着 Marketplace 变化可能增减；如要保持最新，需要重跑同步脚本。
  - 当前集成的是 Marketplace 目录元数据和正文提取文本，不是把 98 个模板逐个转写成 98 份本地 Markdown 成品；如果后续需要，我可以继续把这 98 条按主题拆成多套本地模板资产。

## 2026-03-13 项目启动全局规则对齐与 Skills 索引建立
- 阶段目标：落实全局协作规则，在项目起步阶段完成可用/相关 skills 检索并沉淀复用索引。
- 已完成事项：
  - 复核目录约定，确认 `doc/` 与 `Readme/` 目录已存在并可持续维护。
  - 检索项目内与用户级可用 skills，形成统一启动清单。
  - 新增 `doc/skills索引-项目启动.md`，沉淀“技能清单 + 场景映射 + 复用规则”，便于后续快速提取。
- 涉及文件：
  - `doc/skills索引-项目启动.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - skills 列表会随本机环境变动，后续如新增/卸载 skills，需要同步更新本索引。
  - 当后续对话产物需要交付他人时，需按约定在 `Readme/` 下补充 `使用说明-<对象>.md` 与 `PRD-<对象>.md`，并继续回写本进展记录。

## 2026-03-13 技能发现流程对齐（find-skills）
- 阶段目标：按 `find-skills` 标准流程复核“项目启动技能检索”动作，确保后续可持续扩展 skills。
- 已完成事项：
  - 读取 `C:/Users/AEboli/.codex/skills/find-skills/SKILL.md`，确认该类需求应归入“技能发现/扩展能力”场景。
  - 对齐后续扩展命令约定：`npx skills find <query>`、`npx skills add <owner/repo@skill>`、`npx skills check`、`npx skills update`。
  - 将当前项目优先采用“先用本地已安装 skills，再按需增量检索安装”的策略。
- 涉及文件：
  - `C:/Users/AEboli/.codex/skills/find-skills/SKILL.md`
  - `doc/进展记录.md`
- 待办事项或风险：
  - 如后续出现本地无对应能力的任务，再触发在线检索并评估是否安装新 skill，避免一开始引入过多无关能力。

## 2026-03-13 当前工作区“项目数量”核查
- 阶段目标：确认当前工作目录下有几个实际需要维护的项目（区分源码项目与构建产物/临时目录）。
- 已完成事项：
  - 以“项目 = 可维护的源码仓库/独立包”为口径，扫描 `.git` 与 `package.json`。
  - 排除构建与临时目录（`.next/`、`release/`、`tmp/`、`node_modules/`）后，确认仅存在 1 个主项目：`C:/Users/AEboli/Documents/Codex`（单一 `.git` + 单一 `package.json`）。
  - 识别到构建产物中存在嵌套仓库/包结构：`.next/standalone/.git`、`tmp/installed-*/.git` 以及多处 `.next/**/package.json`，这些属于打包/安装产物，不应计入“需要维护的项目”。
- 涉及文件：
  - `doc/进展记录.md`
- 待办事项或风险：
  - 后续统计“项目数/包数”时需要先明确口径，避免把 `.next/standalone`、`tmp/installed-*` 误判为多项目或多仓库。
